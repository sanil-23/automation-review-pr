#!/bin/bash
# Start the dashboard
# Usage: dashboard

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

cd "${SCRIPT_DIR}/dashboard"
echo "Starting dashboard at http://localhost:${PORT:-3847}"
node server.js
