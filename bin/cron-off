#!/bin/bash
# Clean up any stale crontab/launchd entries
crontab -l 2>/dev/null | grep -v "cron-pr-review.sh" | crontab - 2>/dev/null
launchctl bootout gui/$(id -u)/com.graycyrus.pr-review 2>/dev/null
echo "All scheduled entries cleared."
