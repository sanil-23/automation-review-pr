#!/bin/bash
# Run the full cron cycle now (discover + review all eligible PRs + judge)
# Usage: cron-run

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

echo "Running full cron cycle..."
bash "${SCRIPT_DIR}/cron-pr-review.sh"
