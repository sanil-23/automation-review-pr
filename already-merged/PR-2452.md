# PR #2452 — feat(composio): add Linear as a native memory provider
- **Author**: @M3gA-Mind
- **Branch**: feat/composio-linear-provider → main
- **Created**: 2026-05-21
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2452
- **Status**: clean
- **Last reviewed commit**: 86acfb0683af5008d5679bd7a3f9e6ba6cbfd0ca
- **Last review date**: 2026-05-21T00:00:00Z

## Review History

### Review 1 — 2026-05-21T00:00:00Z
**Type**: Fresh
**Commit**: 86acfb0683af5008d5679bd7a3f9e6ba6cbfd0ca
**Summary**: Adds Linear as the fifth native Composio memory provider (joining gmail/notion/slack/clickup). Creates `src/openhuman/composio/providers/linear/` with provider.rs, sync.rs, tools.rs, tests.rs. Migrates `LINEAR_CURATED` from `catalogs_productivity.rs` into the provider module. Registers Linear across all 6 standard touchpoints (mod.rs, registry, has_native_provider, native_provider_sync_interval, catalog_for_toolkit, catalog re-export).
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (composio/providers)
**Red flags**: None
**Linked issues**: Closes #2400
**PR-Issue alignment**: Issue #2400 asks for a LinearProvider under `composio/providers/linear/` mirroring clickup, with incremental sync, per-item dedup, cursor-based pagination, privacy-scoped to assigned issues, and all 6 registration touchpoints. PR delivers exactly this. Acceptance criteria met.
**CodeRabbit dedup**: 2 findings, both already addressed in commit 86acfb0: (1) viewer_id move inside pagination loop → fixed with `&viewer_id` borrow, (2) cursor advancement despite persist failures → fixed with `had_persist_failures` guard.
**Resolution actions**: None — CodeRabbit findings already resolved by author.
**Surrounding code patterns checked**: Verified against clickup provider pattern (same module structure, registration touchpoints, sync model). Confirmed LINEAR_CURATED parity — same 21 tools, reordered with LINEAR_LIST_LINEAR_USERS added to support viewer identity resolution.
**Dependency audit**: No new external dependencies — all Composio calls go through existing `ProviderContext::execute` path.
**Test coverage**: 36 unit tests across sync.rs (18 inline) and tests.rs (18) covering extraction helpers, provider metadata, and capability matrix registration. Coverage gate passed (≥80%).
**Impact scan**: Rust core only. No frontend, no Tauri shell changes. `LINEAR_CURATED` re-export removed from `catalogs.rs`/`catalogs_productivity.rs` and repointed via `catalog_for_toolkit` — tool-calling surface unchanged.
**Findings**:
- None — clean PR with solid test coverage and established pattern adherence.
**Action taken**: No review posted — all findings covered by CodeRabbit and already addressed. Moved to to-be-approved.
**GitHub review URL**: N/A
