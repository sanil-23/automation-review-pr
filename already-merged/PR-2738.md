# PR #2738 — fix(tools): make http_request web-fetch work with empty allowed_domains
- **Author**: @M3gA-Mind
- **Branch**: M3gA-Mind:http-request-empty-allowlist → main
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2738
- **Status**: approved
- **Last reviewed commit**: 62fd71fef66afb80606f157fba1d7ccafc9e5347
- **Last review date**: 2026-05-27T11:03:23Z

## Review History

### Review 1 — 2026-05-27T11:03:23Z
**Type**: Fresh
**Commit**: 62fd71fef66afb80606f157fba1d7ccafc9e5347
**Summary**: Single-file fix enabling empty `allowed_domains` to mean "open mode" (any public non-private host permitted) instead of early-bailing with a config error. This unblocks external LLM routing (which prefers the generic `http_request` tool) while maintaining all SSRF guards and fail-closed behavior on misconfigured allowlists.
**Gates**: CI all green (32 checks pass), no conflicts, no failing tests
**Areas changed**: Rust core — url_guard validation logic
**Red flags**: None
**Linked issues**: Closes #2700 — Web fetch fails when using external LLM routing
**PR-Issue alignment**: 
- Issue asked: external routing breaks web-fetch; need consistent behavior
- PR delivers: empty allowlist = open mode (matches external routing expectations), non-empty = strict mode (existing behavior preserved), malformed config fails-closed (via sentinel value)
**CodeRabbit dedup**: Both CodeRabbit findings addressed and confirmed by CodeRabbit:
  1. "Don't treat normalized-away allowlist as explicit open mode" → Solved via sentinel value approach
  2. "Log local/private-host rejection path" → SSRF block logging added with `[url_guard]` prefix
**Resolution actions**: None (no changes requested from review)
**Surrounding code patterns checked**: 
- Reviewed url_guard.rs module header comments
- Verified validate_url contract matches new two-mode design
- Checked normalize_allowed_domains fail-closed approach
**Dependency audit**: N/A (no dependencies changed)
**Test coverage**: Excellent
  - New tests: `validate_empty_allowlist_allows_public_host`, `validate_empty_allowlist_still_blocks_private_hosts`, `normalize_all_invalid_entries_stays_fail_closed`, `normalize_empty_input_stays_empty_for_open_mode`, `dns_check_with_empty_allowlist_allows_public_resolved_host`, `dns_check_with_empty_allowlist_blocks_private_resolved_ip`
  - All 49 existing url_guard tests still pass
  - Coverage gate: 80% ✓ (diff-cover)
**Impact scan**:
  - Backward compatible: External routing now works (was broken), default routing unaffected
  - Security: SSRF guards remain in both open and strict modes, DNS rebinding protection confirmed
  - Breaking change: None (fixes a regression, improves behavior)
**Findings**:
- None — code is clean, tests are comprehensive, logging is thorough

**Action taken**: APPROVE → moving to approved/
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2738#pullrequestreview-4371847130
