# PR #2736 — fix(security): gate all cron/schedule mutation tools behind ApprovalGate (GHSA-f46p-6vf9-64mm)

- **Author**: @M3gA-Mind
- **Branch**: M3gA-Mind/ghsa-f46p-approval-gate → main
- **Created**: 2026-05-27T10:41:31Z
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2736
- **Status**: approved
- **Last reviewed commit**: ef9b399ed5fc82790aa3ceda6d85ea7baedecd2e
- **Last review date**: 2026-05-27T10:41:31Z

## Review History

### Review 1 — 2026-05-27T10:41:31Z

**Type**: Fresh  
**Commit**: ef9b399ed5fc82790aa3ceda6d85ea7baedecd2e  
**Summary**: Fixes GHSA-f46p-6vf9-64mm by gating all cron and scheduler mutations behind the ApprovalGate. Adds per-call permission checks to the harness, declares external effects on cron_add/update/run/remove tools, implements args-aware permission and external-effect checks for the schedule tool (allowing read-only list/get while gating all mutations).

**Gates**:
- ✅ CI pass (all 23 checks green)
- ✅ No merge conflicts
- ✅ No prior `graycyrus` feedback

**Areas changed**: Rust core (harness, tool traits, cron and schedule tools, tests)

**Red flags**: None

**Linked issues**: #2723 (critical security issue)

**PR-Issue alignment**: 
- Issue #2723 describes unauthorized job creation via inbound channel messages
- PR correctly fixes the vulnerability by:
  1. Adding per-call permission checks in harness before tool execution
  2. Declaring external effects on all mutating tools
  3. Implementing args-aware permission levels to preserve read-only access for list/get while gating mutations
- Acceptance criteria met: all cron and schedule mutations now require explicit approval

**CodeRabbit dedup**: 
CodeRabbit flagged one issue: `permission_level()` was over-restricting read-only schedule actions. Author addressed this with args-aware `permission_level_with_args()`, which CodeRabbit confirmed as correct. No other CodeRabbit findings remain.

**Resolution actions**: 
- CodeRabbit concern resolved by M3gA-Mind: `permission_level()` returns ReadOnly (minimum), `permission_level_with_args()` enforces exact per-action level at execution time

**Surrounding code patterns checked**: 
- Reviewed trait.rs implementation of new `permission_level_with_args()` default
- Checked turn.rs harness integration against approval gate enforcement point
- Verified cron tool implementations follow Rust style and error handling conventions
- Confirmed schedule tool args-aware logic handles unknown actions correctly (fail-closed)

**Dependency audit**: N/A (no dependencies changed)

**Test coverage**: 
- All 5 changed tools have unit tests
- Coverage gate passed (diff-cover ≥ 80%)
- Tests specifically verify GHSA-f46p-6vf9-64mm protections
- Schedule tool tests cover all actions including unknown action fallback (defaults to Execute/true)

**Impact scan**: 
- New `permission_level_with_args()` trait method is backward-compatible (has default impl)
- Existing tools unaffected by default
- Cross-cutting: affects harness and tool execution path, but only adds gating — no behavioral change for correctly-gated tools
- Schedule tool: allows list/get on read-only channels (improvement), gates all mutations (fix)

**Findings**: None — code is clean, well-tested, security-focused.

**Action taken**: APPROVED

**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2736#pullrequestreview-4371710145
