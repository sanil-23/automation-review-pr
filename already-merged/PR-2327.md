# PR #2327 — fix(onboarding): capture completeAndExit rejection in Sentry (#2081)
- **Author**: @sanil-23
- **Branch**: fix/onboarding-sentry-capture → main
- **Created**: 2026-05-20
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2327
- **Status**: approved
- **Last reviewed commit**: 5c99e713f8e057c70f173e5ae9bf4a37573e3e2d
- **Last review date**: 2026-05-20T12:00:00Z

## Review History

### Review 1 — 2026-05-20T12:00:00Z
**Type**: Fresh
**Commit**: 5c99e713f8e057c70f173e5ae9bf4a37573e3e2d
**Summary**: Adds two `Sentry.captureException` calls to the onboarding completion flow — one in the manual click handler (`ContextPage.tsx`) and one in the auto-advance timer (`ContextGatheringStep.tsx`). Both tag with `flow: 'onboarding-complete'` and distinct `step` values. New test file `ContextPage.test.tsx` covers both reject and resolve paths; existing `ContextGatheringStep.test.tsx` extended with an auto-advance rejection test.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Frontend (onboarding)
**Red flags**: None — PR description mentions `--no-verify` push due to local `ripgrep` missing for `lint:commands-tokens`, but CI lint passed. Acceptable.
**Linked issues**: Refs #2081 (silent dead "Continue to chat" button)
**PR-Issue alignment**: PR does NOT claim to close #2081 (correct — this is instrumentation only, not a UX fix). Adds telemetry to measure whether #2179 and #2177 have resolved the symptom. Aligned with issue goals.
**CodeRabbit dedup**: No inline comments from CodeRabbit — nothing to skip.
**Resolution actions**: None — fresh review, no prior threads.
**Surrounding code patterns checked**: None needed — changes are minimal and self-contained (two Sentry capture calls in existing catch blocks).
**Dependency audit**: No new dependencies — `@sentry/react` already a direct app dependency.
**Test coverage**: New `ContextPage.test.tsx` (reject + resolve paths), extended `ContextGatheringStep.test.tsx` (auto-advance reject path). Coverage gate passed at ≥80%.
**Impact scan**: Desktop only. No runtime overhead on success path. Consent gate in `analytics.ts:beforeSend` applies. No exported API changes.
**Findings**:
- None — clean, well-scoped instrumentation PR with proper tests and documentation.
**Action taken**: COMMENT — LGTM, moved to to-be-approved
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2327#pullrequestreview-4328757547

### Approved — 2026-05-20T13:13:52.973Z
**Approved by**: graycyrus
**Pre-flight**: CI pass | No conflicts
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2327#pullrequestreview-4328785200
