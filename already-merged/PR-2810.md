# PR #2810 — fix: resolve Sentry issue 5677
- **Author**: @CodeGhost21
- **Branch**: fix/sentry-tauri-rust-4zd → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2810
- **Status**: approved
- **Last reviewed commit**: 914506555a40f5874bc6b538c8cdc7a0e35a6142
- **Last review date**: 2026-05-28T07:34:25Z

## AI Summary
- **What it does**: Adds one substring anchor (`"unexpected eof during handshake"`) to the `is_network_unreachable_message` OR-chain in `src/core/observability.rs` so that native-tls's rendering of a TCP connection closed mid-TLS-handshake (by a firewall, AV, or corporate TLS proxy) classifies as `ExpectedErrorKind::NetworkUnreachable` rather than firing a Sentry error event. Includes 3 new tests to validate the classification and pin the rejection contract for data-phase EOFs.
- **Breaking risk**: Zero — purely additive OR-chain entry; no API, type, or export changes.
- **Security risk**: Zero — no user-facing code, no new code paths, no auth surface, no PII.
- **Bottom line**: Safe to merge.

## Review History

### Review 1 — 2026-05-28T07:34:25Z
**Type**: Fresh
**Commit**: 914506555a40f5874bc6b538c8cdc7a0e35a6142
**Summary**: PR fixes Sentry noise issue TAURI-RUST-4ZD where native-tls's "unexpected EOF during handshake" error was escaping the `is_network_unreachable_message` classifier (existing "tls handshake" anchor didn't match because words aren't contiguous). Adds one conservative OR-chain anchor plus 3 tests covering happy path, rejection contract, and emit-site regression guard.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (observability classifier + ws_loop tests)
**Red flags**: PR notes push may have used --no-verify due to missing node_modules in worktree (minor)
**Linked issues**: Sentry TAURI-RUST-4ZD only, no GitHub issue
**PR-Issue alignment**: N/A (Sentry-only)
**CodeRabbit dedup**: CodeRabbit left no inline comments; CR auto-approved. Nothing to dedup.
**Resolution actions**: None
**Surrounding code patterns checked**: `is_network_unreachable_message` OR-chain + sibling anchor entries reviewed in diff
**Dependency audit**: N/A — no new dependencies
**Test coverage**: 3 new tests; happy path (supervisor-wrapped + bare native-tls render), rejection contract (4 non-handshake EOF strings), emit-site guard. Coverage gate CI: pass.
**Impact scan**: Additive OR-chain entry. No callers to update; `expected_error_kind` dispatcher precedence unchanged.
**Findings**: None (no critical, major, or minor)
**Action taken**: APPROVED
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2810#pullrequestreview-4378850192
