# PR #2446 — fix(agent-harness): dedup visible tool specs in all provider-call paths
- **Author**: @YellowSnnowmann
- **Branch**: fix/dedup-tool-specs-all-paths → main
- **Created**: 2026-05-21
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2446
- **Status**: clean
- **Last reviewed commit**: 79f1ef1b97096e20531517ef765ece95a8b542d4
- **Last review date**: 2026-05-21T17:38:36Z

## Review History

### Review 1 — 2026-05-21T17:38:36Z
**Type**: Fresh
**Commit**: 79f1ef1b97096e20531517ef765ece95a8b542d4
**Summary**: Deduplicates `ChatRequest.tools` by name in `run_tool_call_loop` and `subagent_runner::run_inner_loop` — the two provider-call paths not covered by PR #1710. Promotes `dedup_visible_tool_specs` to `pub(crate)`, re-exports from `session::mod`, adds a regression test with `CapturingProvider`, and adds a `tracing::warn!` in subagent_runner when duplicates are dropped. Fixes Sentry TAURI-RUST-4 (2,011 occurrences of HTTP 400 "Tool names must be unique").
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (agent harness — session, tool_loop, subagent_runner)
**Red flags**: None
**Linked issues**: TAURI-RUST-4 (Sentry), related to #1710
**PR-Issue alignment**: Directly addresses the remaining two call paths that #1710 missed
**CodeRabbit dedup**: CodeRabbit flagged dedup precedence mismatch in subagent_runner (schema order vs execution order) — author addressed in commit 79f1ef1. Skipped in our review.
**Resolution actions**: None (first review)
**Surrounding code patterns checked**: `tool_loop.rs` dispatch order (line 640: `tools_registry.chain(extra_tools).find(...)` — matches spec build order), `dedup_visible_tool_specs` function body in `builder.rs` (already logs `log::warn!` internally)
**Dependency audit**: N/A — no new dependencies
**Test coverage**: New regression test `run_tool_call_loop_dedups_duplicate_tool_names_before_provider_call` using `CapturingProvider`; existing 4 `dedup_tests` now cover all three call sites via shared helper
**Impact scan**: Rust core only, no frontend/Tauri-shell/API changes. Visibility widened from `pub(super)` to `pub(crate)` — internal only.
**Findings**:
- [minor] subagent_runner/ops.rs:859 — Double warning: `dedup_visible_tool_specs` already emits `log::warn!` with dropped names; the outer `tracing::warn!` duplicates this and mixes logging facades
**Action taken**: Posted COMMENT review with 1 minor inline finding
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2446#pullrequestreview-4339258479
