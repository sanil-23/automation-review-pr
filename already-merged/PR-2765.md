# PR #2765 — fix(i18n): correct key/value semantic mismatch in chunk-5 locale files
- **Author**: @graycyrus
- **Branch**: fix/i18n-fix-key-value-semantic-mismatch-in-2740 → main
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2765
- **Status**: changes-requested
- **Last reviewed commit**: ea13c5158c1e4343f2a4bd9de113bfa8f07bbf6a
- **Last review date**: 2026-05-27T13:52:40Z

## Review History

### Review 1 — 2026-05-27T13:52:40Z
**Type**: Fresh
**Commit**: ea13c5158c1e4343f2a4bd9de113bfa8f07bbf6a
**Summary**: Fixes 3 semantically inverted translation values (paused/enableEcho/inactive) across 12 locale chunk-5 files (closes #2740). Also bundles a new 525-line DevWorkflowPanel settings component for configuring an autonomous GitHub issue-to-PR agent, composio API additions (listGithubRepos + types), navigation wiring, and Rust Composio action slug corrections to match the current catalog.
**Gates**: CI pending | No conflicts noted | No prior reviews
**Areas changed**: i18n (12 locale chunks + en.ts), Frontend (DevWorkflowPanel.tsx, Settings.tsx, DeveloperOptionsPanel.tsx, useSettingsNavigation.ts), Composio API (composioApi.ts, types.ts), Rust (provider.rs, sync.rs, tests.rs, tools.rs)
**Red flags**: PR title says "fix(i18n)" but bundles a significant new feature and Rust backend changes. Pre-push hook bypassed with --no-verify.
**Linked issues**: Closes #2740
**PR-Issue alignment**: i18n fix portion fully addresses #2740. DevWorkflow panel and Rust slug corrections are additive scope not in the issue.
**CodeRabbit dedup**: CodeRabbit review was in progress (pending) at time of review — no findings to dedup yet.
**Resolution actions**: None
**Surrounding code patterns checked**: DevWorkflowPanel.tsx diff read in full; composioApi.ts diff read; tools.rs diff read.
**Dependency audit**: N/A — no new npm/cargo dependencies added.
**Test coverage**: No tests for DevWorkflowPanel (525-line new component). i18n and Rust slug changes have no logic — N/A for those.
**Impact scan**: listGithubRepos export added to composioApi.ts but not used anywhere (DevWorkflowPanel calls composioExecute directly). Dead export. Rust slug renames are self-contained; tests updated to match.
**Findings**:
- [major] app/src/lib/composio/composioApi.ts:207 — `listGithubRepos` exported but never imported or called; panel uses `composioExecute` directly with the same action. Dead code that wraps a different RPC method, causes confusion.
- [major] app/src/components/settings/panels/DevWorkflowPanel.tsx:67 — `JSON.parse(raw) as DevWorkflowConfig` with no field validation; schema changes silently produce undefined values at runtime.
- [major] app/src/components/settings/panels/DevWorkflowPanel.tsx — No tests for new 525-line component with multiple async paths, fork detection, and error states.
- [minor] app/src/components/settings/panels/DevWorkflowPanel.tsx:516 — "Phase 2: This will automatically create a cron job..." is a WIP placeholder shipping in production UI.
**Action taken**: COMMENT (CI pending; 3 major findings)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2765#pullrequestreview-4372902064

## AI Summary
**What it does**: Fixes 3 semantically inverted translation values across 12 locale chunk files (closes #2740); adds a DevWorkflow settings panel for configuring an autonomous GitHub issue-to-PR agent via Composio GitHub connection; corrects Composio action slugs in Rust to match the current catalog and removes 3 deprecated tool entries.

**Breaking risk**: Low. Key names unchanged, new route additive, Rust slug renames fix incorrect names.

**Security risk**: Low. DevWorkflowPanel persists only repo name/branch/schedule to localStorage — no credentials. All GitHub data flows through existing Composio connection layer.

**Bottom line**: Safe to merge once CI passes and the dead export, JSON cast validation, and missing tests are addressed.
