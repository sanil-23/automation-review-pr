# PR #2025 — feat(core): desktop companion domain — session, pipeline, pointing (#1909)
- **Author**: @yuvrxj-afk
- **Branch**: (feature branch) → main
- **Created**: 2026-05-18
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2025
- **Status**: changes-requested
- **Last reviewed commit**: e02d578b
- **Last review date**: 2026-05-18T10:00:52Z

## Review History

### Review 1 — 2026-05-18T10:00:52Z
**Type**: Fresh
**Commit**: e02d578b
**Summary**: New `desktop_companion` Rust domain (2657 lines, 17 files, all additions) implementing a Clicky-style interaction loop. Adds session lifecycle with consent gate and TTL, STT→LLM→TTS pipeline with cancellation, `[POINT:x,y:label:screenN]` parser with multi-monitor mapping, provider-surface handoff, broadcast bus, 5 RPC controllers, and 66 tests. PR 1 of 2 — Tauri bridge + React UI deferred.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass (all 12 CodeRabbit threads resolved)
**Areas changed**: Rust core, Event bus, Documentation, Tests
**Red flags**: Very large diff (2657 lines) but justified — all new files for a new domain module. Partial close of #1909 (honestly scoped).
**Linked issues**: #1909
**PR-Issue alignment**: Good — PR honestly scopes as PR 1 of 2, no overclaims. Deferred acceptance criteria (Tauri bridge, React UI) are transparently noted.
**CodeRabbit dedup**: All 12 CodeRabbit threads resolved by author across fixup commits. No remaining findings to dedup — all were addressed (session auto-expire, TTL overflow guard, error-path logging, testable handoff helper, token-aware keyword matching, cancellation check before STT, cancelled_result Idle restore, namespace_description, doc fixes, config_set round-trip test).
**Resolution actions**: None — fresh review, no prior graycyrus threads.
**Surrounding code patterns checked**: overlay/bus.rs (bus pattern, LOG_PREFIX), meet_agent/mod.rs (pipeline reference), provider_surfaces/store.rs + types.rs (store pattern, PartialEq, deny_unknown_fields), core/all.rs (controller registration)
**Dependency audit**: N/A — no dependency changes
**Test coverage**: 66 tests (65 unit + 1 E2E). Gaps: no run_audio_turn happy path test, no Thinking→Pointing explicit transition test, config_set always errors (no positive path possible yet).
**Impact scan**: Additive only — new exports, new DomainEvent variants, new namespace "companion". No existing callers affected.
**Findings**:
- [major] types.rs — Missing `PartialEq` on domain types (CompanionConfig, ConversationTurn, CompanionSessionStatus, params/results). Sibling modules derive it on all domain types.
- [major] types.rs — Missing `#[serde(deny_unknown_fields)]` on `StartCompanionSessionParams` and `StopCompanionSessionParams`. Sibling `provider_surfaces` applies this to all inbound types.
- [major] session.rs:108-110 — TOCTOU race in `session_status()` TTL expiry handling. Between `drop(guard)` and `stop_session()`, another thread could start a new session which would then be killed.
- [minor] pipeline.rs:48 — `run_text_turn`/`run_audio_turn` don't document the Listening state precondition. Will error if caller doesn't transition to Listening first.
**Action taken**: Posted REQUEST_CHANGES
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2025#pullrequestreview-4309143161
