# PR #2416 — fix(tauri): pre-flight every xdg-utils binary before register_all (#5V)
- **Author**: @oxoxDev
- **Branch**: fix/sentry-5v-xdg-utils-preflight → main
- **Created**: 2026-05-21
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2416
- **Status**: approved
- **Last reviewed commit**: f4e6a4f80a4688cf64a83007f54285206c90688a
- **Last review date**: 2026-05-21T00:00:00Z

## Review History

### Review 1 — 2026-05-21T00:00:00Z
**Type**: Fresh
**Commit**: f4e6a4f80a4688cf64a83007f54285206c90688a
**Summary**: Extends the Linux deep-link pre-flight from checking only `xdg-mime` to all three binaries (`xdg-mime`, `update-desktop-database`, `xdg-icon-resource`) that `tauri-plugin-deep-link::register_all` shells out to. Skips the plugin call when any are missing, logging which ones. Adds a regression test pinning the partial-install case (OPENHUMAN-TAURI-5V, 18 Sentry events).
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Tauri shell (app/src-tauri/src/lib.rs)
**Red flags**: None
**Linked issues**: OPENHUMAN-TAURI-5V (Sentry, not GitHub issue)
**PR-Issue alignment**: PR directly addresses the Sentry issue — partial xdg-utils installs where `xdg-mime` existed but `update-desktop-database` was missing caused noisy internal `log::error!` to escape to Sentry. Fix pre-flights all three binaries.
**CodeRabbit dedup**: No inline comments from CodeRabbit — nothing to dedup
**Resolution actions**: None
**Surrounding code patterns checked**: `path_has_executable` implementation (lib.rs:1039-1044, checks `is_file()` on PATH entries), sibling tests (`path_has_executable_finds_file_on_path`, `_returns_false_when_missing`, `_returns_false_when_path_unset`) — new test follows identical pattern (ENV_LOCK, tempdir, PATH swap/restore)
**Dependency audit**: N/A — no new dependencies
**Test coverage**: New test `path_has_executable_returns_false_for_partial_xdg_utils_install` covers the partial-install branch directly. Existing `path_has_executable_finds_file_on_path` covers the happy path (all binaries present → `missing.is_empty()` → calls `register_all`).
**Impact scan**: Linux-only change. Three stat-based lookups at app startup instead of one. No exported API changes, no cross-cutting impact.
**Findings**: None
**Action taken**: LGTM — no review posted. Moved to to-be-approved.
**GitHub review URL**: N/A

### Approved — 2026-05-21T17:24:07.431Z
**Approved by**: graycyrus
**Pre-flight**: CI pass | No conflicts
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2416#pullrequestreview-4339160742
