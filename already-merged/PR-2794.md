# PR #2794 — fix(inference): distinguish missing-data-field from wrong-type in /models parser (Sentry TAURI-RUST-4Y)
- **Author**: @CodeGhost21
- **Branch**: fix/inference-models-parser-4y → main
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2794
- **Status**: approved
- **Last reviewed commit**: 711301533d5216e56a999c9bdbfd2bdc61f879a3
- **Last review date**: 2026-05-28T05:18:14Z

## Review History

### Review 1 — 2026-05-28T05:18:14Z
**Type**: Fresh
**Commit**: 711301533d5216e56a999c9bdbfd2bdc61f879a3
**Summary**: Fixes Sentry TAURI-RUST-4Y — the `/models` response parser collapsed "missing `data` field" and "`data` present but wrong type" into one misleading error string. Extracts `parse_models_response()` as a pure helper with three explicit failure arms, adds `json_value_kind()` helper, and adds 3 new tests covering happy path + all failure modes. Single file changed: `src/openhuman/inference/provider/ops.rs`.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (`src/openhuman/inference/provider/ops.rs`)
**Red flags**: None
**Linked issues**: Sentry TAURI-RUST-4Y (133 events), complementary to #2785
**PR-Issue alignment**: Direct fix for the exact Sentry issue described
**CodeRabbit dedup**: No inline CodeRabbit comments; bot approved only
**Resolution actions**: None
**Surrounding code patterns checked**: Diff context (filter_map body, RpcOutcome construction)
**Dependency audit**: N/A — no dependency changes
**Test coverage**: 3 new tests; Coverage Gate CI passed (diff-cover >= 80%); happy path + wrong-type + non-object-body all covered
**Impact scan**: `parse_models_response` is private; `json_value_kind` is private; no exported surface changed; call site in `list_configured_models_from_config` is the only caller
**Findings**:
- [minor] `src/openhuman/inference/provider/ops.rs` tests — `parse_models_response_handles_non_object_body` only asserts `!err.is_empty()` instead of checking a specific substring like the other two tests do; weak regression protection for this branch
**Action taken**: APPROVED
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2794#pullrequestreview-4378079319

## AI Summary
**What it does**: Splits a single ambiguous error path in the OpenAI `/models` response parser into three distinct, actionable error messages — one for non-object top-level body, one for missing `data` field, and one for `data` present but not an array. Addresses the core confusing Sentry fingerprint where the error said "data missing" while the keys list included `data`.
**Breaking risk**: Zero. Pure internal refactor; no exported types, no RPC signatures, no config formats changed.
**Security risk**: Zero. No new I/O, no user-controlled code paths, no secrets or auth logic touched.
**Bottom line**: Safe to merge.
