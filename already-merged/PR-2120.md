# PR #2120 — fix(core): skip memory init on config failure instead of fabricating a default workspace (OPENHUMAN-CORE-48)
- **Author**: @sanil-23
- **Branch**: `fix/sentry-core-48-workspace-fallback` → `main`
- **Created**: 2026-05-18
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2120
- **Status**: approved
- **Last reviewed commit**: fe5bf13570a4db230077fd389703de07ac1f09c7
- **Last review date**: 2026-05-19T06:49:00Z

## Review History

### Review 1 — 2026-05-19T06:49:00Z
**Type**: Fresh
**Commit**: fe5bf13570a4db230077fd389703de07ac1f09c7
**Summary**: Single-file change to `src/core/jsonrpc.rs` restructuring the boot block so that when `Config::load_or_init()` fails, the memory and whatsapp_data global singletons are left uninitialised instead of being initialised against a fabricated `Config::default()` workspace. Fixes silent data integrity issue (chunk loss / cross-workspace bleed-over) on headless/containerised deploys with no writable `$HOME`.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core
**Red flags**: No linked GitHub issue (tracked via Sentry OPENHUMAN-CORE-48 — acceptable for production bugfix). Author pushed with `--no-verify` (unrelated Tauri-shell pre-push hook).
**Linked issues**: Sentry OPENHUMAN-CORE-48
**PR-Issue alignment**: Clean match — PR description, Sentry issue, and code all align. Change is precisely scoped with no overclaim.
**CodeRabbit dedup**: CodeRabbit approved with no findings — full review conducted independently.
**Resolution actions**: None (fresh review, no prior threads)
**Surrounding code patterns checked**: `src/core/mod.rs`, `logging.rs`, `shutdown.rs`, `memory/global.rs`, `whatsapp_data/global.rs` — PR follows all conventions: `[boot]` log prefix, correct log levels (error/info/warn), match-based error handling, snake_case naming, crate-relative imports.
**Dependency audit**: N/A — no dependency changes
**Test coverage**: Acceptable — process-global singleton boot code is inherently hard to unit test. Success path is byte-for-byte unchanged (existing tests cover it). Failure path removes a code path rather than adding complex logic; correctness verifiable by inspection.
**Impact scan**: No exported function signatures, RPC methods, types, or Redux state changed. The "uninitialised" singleton state is an explicitly designed and documented mode — callers already handle `client()` returning `Err` and `client_if_ready()` returning `None`.
**Findings**:
- No critical, major, or minor issues found
**Action taken**: No issues found — moved to to-be-approved/
**GitHub review URL**: N/A

### Approved — 2026-05-19T13:24:24.222Z
**Approved by**: graycyrus
**Pre-flight**: CI pass | No conflicts
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2120#pullrequestreview-4319345200
