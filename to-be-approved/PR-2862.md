# PR #2862 — feat(tauri): OPENHUMAN_FORCE_GPU env override for Linux CEF disable
- **Author**: @zahica1234
- **Branch**: feat/force-gpu-env-override → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2862
- **Status**: clean
- **Last reviewed commit**: c4447f15311e85a9b40206baaa2b79c3e89bdf96
- **Last review date**: 2026-05-29T00:00:00Z

## Review History

### Review 1 — 2026-05-29T00:00:00Z
**Type**: Fresh
**Commit**: c4447f15311e85a9b40206baaa2b79c3e89bdf96
**Summary**: Adds `OPENHUMAN_FORCE_GPU=1` env override that suppresses the unconditional Linux `--disable-gpu` / `--disable-gpu-compositing` flags (issue #1697 workaround), re-enabling WebGL2 so the Rive mascot on the `/human` tab renders. Mirrors the `OPENHUMAN_CEF_PREWARM` override pattern. One file changed: `app/src-tauri/src/lib.rs` (112 additions, 10 deletions).
**Gates**: CI FAILING (pre-existing unrelated test `core::legacy_aliases::tests::frontend_legacy_aliases_match_server_alias_table`; PR only touches `app/src-tauri/src/lib.rs`) | Conflicts pass | No prior feedback
**Areas changed**: Tauri shell (`app/src-tauri/src/lib.rs`)
**Red flags**: Fork is behind upstream main (base commit 6d0657e4); author cannot rebase due to missing `workflow` scope on `gh repo sync`
**Linked issues**: #1697 (Linux EGL_NOT_INITIALIZED CEF abort), #1012 (Intel macOS crash), #2463 (Wayland guard referenced in context)
**PR-Issue alignment**: Directly addresses side-effect of #1697 workaround; adds opt-in escape hatch, default behavior unchanged
**CodeRabbit dedup**: CodeRabbit flagged `is_empty()` assertion in `platform_cef_gpu_workarounds_skip_linux_disable_when_force_gpu_set` (root-Linux CI spurious failure risk) — already addressed in final commit c4447f1; not repeated
**Resolution actions**: None (CodeRabbit suggestion already incorporated)
**Surrounding code patterns checked**: `cef_prewarm_enabled` at lib.rs:1620 — confirmed new function follows same truthy-string parsing convention; control flow is correctly inverted (opt-in vs opt-out)
**Dependency audit**: N/A — no dependency changes
**Test coverage**: 5 new tests covering full override matrix + 3 existing tests updated with new `None` parameter. Coverage: default-off, truthy values, falsy values, Linux suppression, Intel-macOS orthogonality. Solid.
**Impact scan**: `append_platform_cef_gpu_workarounds` is private; no external callers. `run()` call site updated. No public API change.
**Findings**:
- [minor] lib.rs doc comment on `cef_force_gpu_enabled` says "mirrors the truthy/falsy parsing in `cef_prewarm_enabled`" — slightly misleading since the control flow is inverted (prewarm is opt-out via explicit-falsy; this is opt-in via explicit-truthy). Behavior is correct, comment just needs a word of clarification. Not blocking.
**Action taken**: COMMENT — CI failing (pre-existing unrelated failure + fork behind upstream), code clean. Moved to to-be-approved pending CI green + rebase.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2862#pullrequestreview-4383867003

## AI Summary
**What it does**: Adds an opt-in `OPENHUMAN_FORCE_GPU=1` environment variable that suppresses the unconditional Linux CEF GPU-disable flags added as the #1697 workaround, allowing users with working GPU stacks to get WebGL2 and the Rive mascot. Default behavior (GPU disabled on Linux) is unchanged — only explicit truthy values opt in.
**Breaking risk**: Zero. `append_platform_cef_gpu_workarounds` is a private function; no exported API, type, or config format changed. New env var defaults off.
**Security risk**: Zero. Reading an env var for a user-opt-in display feature. No injection surface, no credentials, no network access. Garbage input falls to safe default.
**Bottom line**: Safe to merge — blocked only by pre-existing CI failure and the fork being behind upstream, not by code quality.
