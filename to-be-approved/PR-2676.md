# PR #2676 — feat(chat): add image attachment support to composer
- **Author**: @rsd-darshan
- **Branch**: rsd-darshan/chat-image-attachments → main
- **Created**: 2026-05-??
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2676
- **Status**: clean
- **Last reviewed commit**: ecf1fb572e5bb30f4ac59941cf8a358210a21917
- **Last review date**: 2026-05-28T18:25:32Z

## Review History

### Review 1 — 2026-05-28T18:25:32Z
**Type**: Continuation (addressing prior CHANGES_REQUESTED feedback)
**Commit**: ecf1fb572e5bb30f4ac59941cf8a358210a21917
**Summary**: Frontend implementation of multimodal chat image attachments. New library module for validation, FileReader, message composition/parsing, and formatting. New AttachmentPreview component for thumbnail chips. Conversations.tsx wired with attachment state, file input, composer button, send logic, and bubble rendering. 19 unit tests covering validation, parsing, and formatting. i18n keys added across 13 locale files. 1485 additions, 37 deletions.

**Gates**: 
- CI: 100% GREEN ✅
- Conflicts: None ✅
- Prior graycyrus feedback: ADDRESSED ✅

**Areas changed**: 
- Frontend: attachments library, AttachmentPreview component, Conversations page
- Tests: 19 unit tests for attachments utility, 6 tests for component
- Config: i18n keys across 13 locale files
- API: chatSendError.ts — new 'attachment_invalid' error code

**Red flags**: None

**Linked issues**: #2662 (Multimodal attachments)

**PR-Issue alignment**: 
- Issue asks for image upload, camera capture, file upload, attachment preview, model routing, memory integration, failure states, privacy controls, regression safety, 80% coverage
- PR implements image upload + attachment preview + validation errors + existing backend routing via [IMAGE:] markers
- PR is scoped Phase 1: desktop image attachment only (not camera, file, memory, privacy UI — deferred to follow-ups)
- Acceptance criteria partially met: image upload ✅, attachment preview ✅, failure states ✅, unit test coverage ✅
- Out of scope for this PR: camera capture, file upload, memory integration, privacy controls (acknowledged in design)

**CodeRabbit dedup**: 
CodeRabbit identified a batch attachment count validation bug (stale length when looping through file selection). This has been fixed in the current commit: `handleAttachFiles` now uses `acceptedCount` variable incremented as each file is accepted, ensuring validation sees cumulative count.

**Resolution actions**: 
Prior graycyrus CHANGES_REQUESTED addressed:
- [major] Image rendering fallback: ✅ Implemented. Rendering now uses `parseMessageImages(msg.content)` as fallback when `msg.extraMetadata.attachmentDataUris` is unavailable, ensuring persisted messages still display images on reload.
- [minor] Non-English i18n translations: ⚠️ Still English placeholders. Acceptable as follow-up; does not block this release.

**Surrounding code patterns checked**: 
- Attachment state management: follows React hooks pattern (useState, useRef)
- Error handling: new `attachError` state parallels existing `sendError`
- Message composition: integrates buildMessageWithAttachments() cleanly into existing send flow
- File validation: solid type safety and error codes
- Component: AttachmentPreview follows existing chip/button patterns in project

**Dependency audit**: No new dependencies. Uses built-in FileReader, crypto.randomUUID(), File/Blob APIs.

**Test coverage**: 
- attachments.ts: 19 unit tests (validation, formatting, parsing, composition) — >80%
- AttachmentPreview.tsx: 6 component tests (render empty, render chips, thumbnails, remove, disabled, multiple)
- Conversations.tsx: E2E covered by existing chat test suite (send with attachments, clear after send)
- Overall diff coverage: ✅ Passes coverage gate (diff-cover ≥ 80%)

**Impact scan**:
- New exported functions: isAllowedMimeType, fileToDataUri, validateAndReadFile, buildMessageWithAttachments, parseMessageImages, formatFileSize — all in new module, no existing callers
- ChatSendError updated with new 'attachment_invalid' code — isolated to attachments error path
- Conversations.tsx: attachment state added to send logic, no breaking changes to existing send path
- No RPC changes, no shared type rework, no Redux state changes

**Findings**:
- [critical] None
- [major] None (prior major issue now fixed: image rendering fallback implemented)
- [minor] Non-English locales have English placeholder translations (acceptable follow-up)

**Action taken**: Posted COMMENT clearing prior CHANGES_REQUESTED. Major blocking issue has been resolved. PR is now ready for approval/merge.

**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2676#pullrequestreview-4383617339

---

## AI Summary

**What it does**: Adds image attachment support to desktop chat. Users can click a paperclip button in the composer, select image files (PNG, JPEG, WebP, GIF, BMP), see thumbnail previews with per-item remove buttons, and send images alongside text. Images are encoded as `[IMAGE:<data-uri>]` markers in the message, which the existing Rust backend (`agent/multimodal.rs`) already parses and routes to the inference provider — no backend changes needed. Supports up to 4 images per message, 8 MB each. Validation errors (unsupported type, oversized, count exceeded) surface in the composer error banner. Supports send-with-attachments-only (empty text + images). Attachments clear after send.

**Breaking risk**: Zero. No new exports that could break callers, no changes to existing APIs, no RPC changes, no Redux state reshaping.

**Security risk**: Low. 
- FileReader.readAsDataURL() only processes client-selected files (no network source).
- Data URIs embedded in message text — no script injection risk (treated as plain text by backend).
- MIME type validation prevents non-image files.
- Size limit (8 MB) prevents memory exhaustion attacks.
- No hardcoded secrets, credentials, or PII in logs.

**Bottom line**: Safe to merge — all prior feedback addressed, CI green, solid test coverage, clean architecture, no breaking changes.
