# Intelligence Context — PR #2210

## PR Metadata
- **Title**: feat(local-ai): add editable Ollama server URL with connection test
- **Author**: @M3gA-Mind
- **Branch**: feat/ollama-external-server-url → main
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2210
- **Review type**: Fresh
- **Latest commit**: 0a8bf1d909c39ded7b35fd18e441407297ecc65b
- **Commit count**: 7
- **Diff size**: 38 files, +1577 -264 lines
- **Labels**: feature
- **Reviewers**: coderabbitai (Approved), tinyhumansai/maintainers (Requested)

## Commits
1. `0f076cf` — fix(security): always canonicalize paths before policy check
2. `be29669` — fix(security): resolve forbidden paths against workspace root; validate…
3. `61343cf` — fix(review): unwrap double-wrapped error in image_info; add relative…
4. `60d7a0f` — test(security): cover validate_parent_path forbidden-path block
5. `2011507` — feat(local-ai): add editable Ollama server URL with connection test
6. `fba0d51` — fix(local-ai): address CodeRabbit review comments
7. `0a8bf1d` — test(local-ai): add coverage for Ollama URL panel, connection test, a…

## PR Description
- `ollama_base_url()` previously read only env vars and ignored `config.local_ai.base_url`; adds `ollama_base_url_from_config(config)` with correct priority chain (config → env → default) and updates bootstrap/health-check/infer/vision-embed callsites
- Registers new RPC `openhuman.local_ai_test_connection` that probes `/api/tags` with a 3-second timeout and returns `{reachable, error, models_count}`
- Adds an editable "Ollama Server URL" section to `LocalModelDebugPanel` / `ModelStatusSection`: text input, inline validation, Test Connection button, Save/Reset, seeded from persisted config on mount
- Adds `validate_ollama_url` in both Rust and TypeScript (scheme + host + creds + path normalization)
- Adds i18n keys (`localModel.ollamaServer.*`) across all 12 locale chunks

## Red Flags
- **Multiple unrelated changes bundled**: The PR bundles two separate concerns — (1) security/path validation refactoring (commits 1-4: `validate_path`/`validate_parent_path`, renaming `is_path_allowed` → `is_path_string_allowed`, refactoring filesystem tools) and (2) Ollama URL configuration feature (commits 5-7). These should arguably be separate PRs.
- **Large diff (1577 additions)**: Justified by scope — i18n across 12 locales accounts for significant boilerplate, plus test coverage and the security refactor.
- **7 commits with mixed concerns**: Security fixes and feature work mixed in one PR.

## Linked Issues + Acceptance Criteria
### Issue #2159 — "Let users configure an external Ollama server URL"
**Author**: @Al629176 | **Assignee**: @M3gA-Mind | **State**: OPEN

Acceptance criteria:
- [x] **Editable URL field** — Local Model settings include an editable `Ollama server URL` input backed by `local_ai.base_url`
- [x] **Validation included** — The input validates URL format and gives clear guidance for host/port values
- [x] **Connection test works** — Users can test the configured Ollama server and see reachable/unreachable status
- [x] **External server respected** — If the configured Ollama server is reachable, OpenHuman uses it without requiring local binary bootstrap
- [x] **Install bootstrap skipped** — `Fix binary path` or related repair flows do not force a fresh install when an external Ollama server is configured and healthy
- [x] **Config persists** — Saved server URL persists through app restart and updates `config.toml`/core config correctly
- [x] **Read-only display updated** — Existing read-only display in `ModelStatusSection.tsx` is replaced or paired with the editable setting
- [x] **Regression safety** — Unit/integration coverage verifies URL editing, config persistence, connection testing, and skip-bootstrap behavior
- [x] **Diff coverage ≥ 80%** — the fix PR meets the changed-lines coverage gate

## PR-Issue Alignment (three-way verification)
**Match**: The PR description and code changes align well with Issue #2159's acceptance criteria. The PR implements all acceptance criteria.

**Scope additions beyond the issue**:
- Security/path validation refactoring (commits 1-4) — `validate_path`/`validate_parent_path` consolidation, renaming `is_path_allowed` → `is_path_string_allowed`, refactoring all filesystem tool implementations. This is **not** related to Issue #2159 and is bundled scope creep from a prior security fix branch.
- `.claude/memory.md` updates — internal AI-agent notes, low impact.

**No BLOCKING mismatches**: The Ollama URL feature implementation fully addresses the issue.

## Prior Review Context (continuation only)
N/A — Fresh review.

## Review Feedback + Resolution Candidates
### CodeRabbit Review
CodeRabbit submitted an initial CHANGES_REQUESTED review, then the author addressed comments in two follow-up commits (`fba0d51` and `0a8bf1d`). CodeRabbit then approved.

**All 21 review threads are RESOLVED.** Summary of what was flagged and addressed:

#### Addressed by author:
- `.claude/memory.md:154` — Conflicting test-run guidance → Author dismissed (internal scratch file, not user-facing)
- `ModelStatusSection.tsx:114` — `canTest` should check `urlValidation.valid` → **Fixed** in `fba0d51`
- `ModelStatusSection.tsx:203` — Hardcoded English model count → **Fixed** with i18n key `localModel.ollamaServer.modelCount`
- `LocalModelDebugPanel.tsx:321` — Diagnostics overwrites unsaved URL edits → **Fixed** with conditional update pattern
- `LocalModelDebugPanel.tsx:351` — Raw URL logging leaks credentials → **Fixed** with `replace(/\/\/[^@]*@/, '//***@')`
- All 11 i18n locale files — Untranslated English strings → **All translated** in `fba0d51`
- `ollama_admin.rs:1256` — `has_model()` ignores config base_url → Author dismissed (descoped, follow-up planned)
- `public_infer.rs:570` — Raw base_url in debug log → **Fixed** with `redact_ollama_base_url`
- `csv_export.rs:197` — `record_action()` ordering → Author dismissed (pre-existing, out of scope)
- `file_write.rs:76` — `record_action()` ordering → Author dismissed (pre-existing, out of scope)

#### Outside-diff comments (not addressable inline):
- `vision_embed.rs:71-76` — Raw URL logging (Major) → Partially addressed; `embed_base` is logged directly
- `security/policy.rs:722-755` — Missing Windows `%5c` traversal blocking → Not addressed
- `ollama_admin.rs:1163-1171` — POST vs GET for `/api/tags` probe → **Not addressed** — `ollama_runner_ok_at` still uses POST
- `edit_file.rs:100-117` — Missing raw path gate before `symlink_metadata()` → Not addressed (but `validate_path` now covers this path)

### No human reviewer threads exist (no graycyrus reviews yet).

## Changed Files
| File | Change type |
|------|-------------|
| `.claude/memory.md` | Modified — AI agent notes |
| `app/src/components/settings/panels/LocalModelDebugPanel.tsx` | Modified — URL state, handlers, config seeding |
| `app/src/components/settings/panels/__tests__/LocalModelDebugPanel.test.tsx` | **New** — 8 tests |
| `app/src/components/settings/panels/local-model/ModelStatusSection.test.tsx` | Modified — 12 new tests |
| `app/src/components/settings/panels/local-model/ModelStatusSection.tsx` | Modified — URL input section, validation UI |
| `app/src/lib/i18n/chunks/ar-2.ts` | Modified — Ollama server i18n keys (Arabic) |
| `app/src/lib/i18n/chunks/bn-2.ts` | Modified — i18n keys (Bengali) |
| `app/src/lib/i18n/chunks/en-2.ts` | Modified — i18n keys (English) |
| `app/src/lib/i18n/chunks/es-2.ts` | Modified — i18n keys (Spanish) |
| `app/src/lib/i18n/chunks/fr-2.ts` | Modified — i18n keys (French) |
| `app/src/lib/i18n/chunks/hi-2.ts` | Modified — i18n keys (Hindi) |
| `app/src/lib/i18n/chunks/id-2.ts` | Modified — i18n keys (Indonesian) |
| `app/src/lib/i18n/chunks/it-2.ts` | Modified — i18n keys (Italian) |
| `app/src/lib/i18n/chunks/pt-2.ts` | Modified — i18n keys (Portuguese) |
| `app/src/lib/i18n/chunks/ru-2.ts` | Modified — i18n keys (Russian) |
| `app/src/lib/i18n/chunks/zh-CN-2.ts` | Modified — i18n keys (Chinese) |
| `app/src/lib/i18n/en.ts` | Modified — root i18n imports |
| `app/src/utils/ollamaUrlValidation.test.ts` | **New** — 12 validation tests |
| `app/src/utils/ollamaUrlValidation.ts` | **New** — TS URL validation |
| `app/src/utils/tauriCommands/localAi.test.ts` | **New** — RPC wrapper tests |
| `app/src/utils/tauriCommands/localAi.ts` | Modified — test_connection + update_settings commands |
| `src/openhuman/cron/scheduler.rs` | Modified — `is_path_allowed` → `is_path_string_allowed` |
| `src/openhuman/inference/local/ollama.rs` | Modified — `ollama_base_url_from_config`, `validate_ollama_url` + tests |
| `src/openhuman/inference/local/schemas.rs` | Modified — `local_ai_test_connection` RPC registration |
| `src/openhuman/inference/local/schemas_tests.rs` | Modified — schema test update |
| `src/openhuman/inference/local/service/mod.rs` | Modified — `pub(crate)` visibility for `ollama_admin` |
| `src/openhuman/inference/local/service/ollama_admin.rs` | Modified — config-aware URL resolution, `test_ollama_connection` |
| `src/openhuman/inference/local/service/ollama_admin_tests.rs` | Modified — 4 new connection test tests |
| `src/openhuman/inference/local/service/public_infer.rs` | Modified — config-aware URL, redacted logging |
| `src/openhuman/inference/local/service/vision_embed.rs` | Modified — config-aware URL |
| `src/openhuman/security/policy.rs` | Modified — `validate_path`/`validate_parent_path`, `is_path_string_allowed` rename |
| `src/openhuman/security/policy_tests.rs` | Modified — renamed test assertions, new async validation tests |
| `src/openhuman/tools/impl/browser/image_info.rs` | Modified — uses `validate_path` |
| `src/openhuman/tools/impl/filesystem/apply_patch.rs` | Modified — uses `validate_path` |
| `src/openhuman/tools/impl/filesystem/csv_export.rs` | Modified — uses `validate_parent_path` |
| `src/openhuman/tools/impl/filesystem/edit_file.rs` | Modified — uses `validate_path` |
| `src/openhuman/tools/impl/filesystem/file_read.rs` | Modified — uses `validate_path` |
| `src/openhuman/tools/impl/filesystem/file_write.rs` | Modified — uses `validate_parent_path` |
| `src/openhuman/tools/impl/filesystem/grep.rs` | Modified — uses `validate_path` |
| `src/openhuman/tools/impl/filesystem/list_files.rs` | Modified — uses `validate_path` |

## Classification + Checklist

### Areas detected: Rust core, Frontend, i18n, Security policy, Tests

#### Rust core (inference/local/*)
- [ ] New functionality in dedicated subdirectory — `ollama_base_url_from_config` and `validate_ollama_url` are in `ollama.rs` (appropriate location)
- [ ] `test_ollama_connection` is a standalone function in `ollama_admin.rs`, not a method on `LocalAiService` — correct for RPC handler access
- [ ] Controller-only exposure via `schemas.rs` registration — correct pattern
- [ ] Debug logging with grep-friendly `[local_ai]` prefixes — consistent
- [ ] No secrets/PII in logs — URL redaction applied in `public_infer.rs`; **vision_embed.rs still logs raw URLs** (CodeRabbit flagged, not fully addressed)

#### Frontend (app/src/*)
- [ ] No dynamic imports — confirmed, no `import()` or `React.lazy`
- [ ] Config read from `config.ts` via `openhumanGetConfig()` — correct pattern
- [ ] State management — local component state (useState), appropriate for settings panel scope
- [ ] Tauri access via RPC client (`callCoreRpc`) — correct pattern, not `window.__TAURI__`

#### i18n
- [ ] All 12 locale chunks updated with `localModel.ollamaServer.*` keys
- [ ] English source in `en-2.ts` — correct
- [ ] All non-English locales translated (not left as English placeholders) — CodeRabbit confirmed all translated in fix commit
- [ ] New key `localModel.ollamaServer.modelCount` added for pluralization

#### Security policy changes
- [ ] `is_path_allowed` renamed to `is_path_string_allowed` — clarifies it's string-only, not I/O
- [ ] New `validate_path` — async, canonicalizes, checks workspace containment + forbidden paths
- [ ] New `validate_parent_path` — for write operations where target file doesn't exist yet
- [ ] `expand_tilde` extracted as method — DRY improvement
- [ ] All filesystem tools migrated from manual `is_path_allowed` + `canonicalize` + `is_resolved_path_allowed` to `validate_path`/`validate_parent_path`

## Surrounding Code Patterns
### src/openhuman/inference/local/
**Files read (from diff)**: `ollama.rs`, `service/ollama_admin.rs`, `service/public_infer.rs`, `service/vision_embed.rs`, `schemas.rs`
- **Naming**: snake_case throughout, `_from_config` suffix for config-aware variants, `_at` suffix for URL-parameterized methods
- **Error handling**: `Result<T, String>` for most functions, `map_err(|e| format!(...))` pattern
- **Imports**: grouped by crate modules then external crates
- **Logging**: `log::debug!("[local_ai] ...")` with grep-friendly prefixes; `tracing::debug!` in some newer code
- **Tests**: Mock Axum server pattern with `spawn_mock()`, `_guard = inference_test_guard()`

### src/openhuman/security/
**Files read (from diff)**: `policy.rs`, `policy_tests.rs`
- **Naming**: `validate_path`/`validate_parent_path`/`is_path_string_allowed`/`is_resolved_path_allowed`
- **Error handling**: `Result<PathBuf, String>` for validation functions
- **Tests**: Unit tests with `default_policy()` helper, `#[cfg(unix)]` gates for symlink tests, `tempfile::tempdir()` for workspace simulation

### app/src/components/settings/panels/
**Files read (from diff)**: `LocalModelDebugPanel.tsx`, `local-model/ModelStatusSection.tsx`
- **Naming**: React PascalCase components, camelCase state/handlers
- **State**: useState hooks, lifted state pattern (state in parent, props to child)
- **i18n**: `useT()` hook, `t('key')` pattern

## CodeRabbit Dedup
CodeRabbit posted 19 actionable inline comments + 1 nitpick + 4 outside-diff comments. All threads resolved. Post-fix review approved.

**Findings already flagged by CodeRabbit (do NOT duplicate):**
- `vision_embed.rs:71-76` — Raw URL logging leaks credentials (Major, outside diff)
- `security/policy.rs:722-755` — Missing Windows `%5c` traversal + `~\` handling (Major, outside diff)
- `ollama_admin.rs:1163-1171` — `/api/tags` probe uses POST instead of GET (Critical, outside diff)
- `edit_file.rs:100-117` — Missing raw path gate before `symlink_metadata()` (Major, outside diff)
- `policy.rs:777-905` — Missing debug/trace logs on reject branches (Nitpick)
- `ModelStatusSection.tsx:114` — `canTest` should include `urlValidation.valid` (Fixed)
- `ModelStatusSection.tsx:203` — Hardcoded English model count (Fixed)
- `LocalModelDebugPanel.tsx:321` — Diagnostics overwrites unsaved edits (Fixed)
- `LocalModelDebugPanel.tsx:351` — Raw URL logging (Fixed)
- All 11 non-English i18n locale files — untranslated strings (All fixed)
- `ollama_admin.rs:1252` — `has_model()` ignores config URL (Dismissed — descoped)
- `public_infer.rs:567` — Raw base_url in debug log (Fixed)
- `csv_export.rs:195` — `record_action()` ordering (Dismissed — pre-existing)
- `file_write.rs:74` — `record_action()` ordering (Dismissed — pre-existing)

## Dependency Audit
N/A — no dependency changes. No `Cargo.toml`, `package.json`, `Cargo.lock`, or `pnpm-lock.yaml` modified.

## Test Coverage
### New tests added:
- **Rust**: 8 tests for `validate_ollama_url`, 2 for `ollama_base_url_from_config`, 4 for `test_ollama_connection` (mock Axum server), 7 for `validate_path`/`validate_parent_path`
- **TypeScript**: 12 tests for `ollamaUrlValidation.ts`, 2 for `localAi.ts` RPC wrapper, 8 for `LocalModelDebugPanel` (integration), 12 for `ModelStatusSection` (URL section)

### Coverage assessment:
- Happy path: well covered — connection success, validation pass, config seeding
- Error paths: covered — connection failure, invalid URLs, server errors
- Edge cases: covered — empty URL, credentials in URL, query strings, fragments
- `validate_path` / `validate_parent_path`: covered with symlink escape, forbidden path, workspace containment tests
- **Missing**: No explicit test for `openhumanUpdateLocalAiSettings` (the save RPC wrapper) — only tested indirectly via the panel test

### PR claims: 2627/2630 Vitest tests pass; Rust tests pass

## Impact Scan
### Changed function signatures:
- `ollama_healthy()` → new `ollama_healthy_at(&str)` wrapper added, old zero-arg preserved for backward compat
- `list_models()` → renamed to `list_models_at(&str)` — **breaking for internal callers** (test updated)
- `ollama_runner_ok()` → renamed to `ollama_runner_ok_at(&str)` — internal only
- `has_model()` → now delegates to `has_model_at(&str)` — backward compat preserved
- `is_path_allowed()` → renamed to `is_path_string_allowed()` — all callers updated
- `external_ollama_request_error()` → new `_with_url` variant added, old preserved

### Changed exports:
- `ollama_admin` module visibility changed from `mod` to `pub(crate) mod` — needed for `test_ollama_connection` access from `schemas.rs`

### New RPC method:
- `openhuman.local_ai_test_connection` — registered in schemas, wired to `test_ollama_connection`

### Frontend:
- `ModelStatusSection` props expanded with 9 new props — all callers updated (only one: `LocalModelDebugPanel`)
- New exports: `OllamaConnectionTestResult`, `openhumanLocalAiTestConnection` from `localAi.ts`
- New utility: `validateOllamaUrl` from `ollamaUrlValidation.ts`

N/A — no Redux state shape changes, no removed exports, no changed RPC method names (only additions).

## Full Diff
The full diff is saved at: `/Users/cyrus/.claude/projects/-Users-cyrus-Desktop-automation-review-pr/5292c279-d6d9-4d19-b15f-8b245af29429/tool-results/blaenufa9.txt`

Due to size (124.8KB, 2846 lines), the diff is referenced by path rather than inlined. The diff covers 38 files across these areas:
1. **Ollama URL feature** (commits 5-7): `ollama.rs`, `ollama_admin.rs`, `public_infer.rs`, `vision_embed.rs`, `schemas.rs`, `LocalModelDebugPanel.tsx`, `ModelStatusSection.tsx`, `ollamaUrlValidation.ts`, `localAi.ts`, 12 i18n chunks
2. **Security policy refactor** (commits 1-4): `policy.rs`, `policy_tests.rs`, `image_info.rs`, `apply_patch.rs`, `csv_export.rs`, `edit_file.rs`, `file_read.rs`, `file_write.rs`, `grep.rs`, `list_files.rs`, `scheduler.rs`
3. **AI agent notes**: `.claude/memory.md`
4. **Test files**: 4 new test files + modifications to 3 existing test files
