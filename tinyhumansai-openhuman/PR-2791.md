# PR #2791 — fix(inference): include model field in nvidia-nim API requests
- **Author**: @graycyrus
- **Branch**: fix/nvidia-nim-model-field → main
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2791
- **Status**: changes-requested
- **Last reviewed commit**: 67d9aec98326bb3a57592ed12c81927000d1901e
- **Last review date**: 2026-05-28T05:20:03Z

## Review History

### Review 1 — 2026-05-28T05:20:03Z
**Type**: Fresh
**Commit**: 67d9aec98326bb3a57592ed12c81927000d1901e
**Summary**: Adds an early error guard in `make_cloud_provider_by_slug` (factory.rs) for empty `effective_model` after fallback, a config rejection classifier for the nvidia-nim `"model field is required"` error body, and three new unit tests in factory_test.rs covering explicit model, default_model fallback, and empty-model error cases.
**Gates**: CI FAILING (test / Rust Core Tests + Quality, Rust Core Coverage) | Conflicts pass | Unresolved CodeRabbit REQUEST_CHANGES
**Areas changed**: Rust core — inference/provider
**Red flags**: CI failure is caused by code in this PR; existing test `openhuman_slug_routes_to_backend` broken by the new guard
**Linked issues**: #2784 (nvidia-nim sends empty model field causing 400s)
**PR-Issue alignment**: Fix direction is correct but implementation has a regression — guard fires for the openhuman slug too. AC1 partially met (guard added but breaks openhuman path), AC2 test added but breaks existing test, AC3/AC4 blocked by CI failure.
**CodeRabbit dedup**: CR flagged `"missing_required_field"` too broad in config_rejection.rs line 159 — skipped, not repeating.
**Resolution actions**: None
**Surrounding code patterns checked**: None (diff-only review; existing test failure output was sufficient)
**Dependency audit**: N/A — no new dependencies
**Test coverage**: 3 tests added in factory_test.rs, but the guard breaks `openhuman_slug_routes_to_backend` (pre-existing test at line 89). Net: regression.
**Impact scan**: `make_cloud_provider_by_slug` is shared across all slug-based providers. The guard at line ~782 fires before the openhuman backend routing logic, causing the openhuman slug to error when no model is in the provider string — which is normal usage.
**Findings**:
- [critical] `src/openhuman/inference/provider/factory.rs:782` — Empty model guard fires for the `openhuman` slug before its backend routing logic runs. Existing test `openhuman_slug_routes_to_backend` panics. Fix: gate on `entry.auth_style != AuthStyle::OpenhumanJwt` or move guard after the openhuman routing block.
**Action taken**: COMMENT (CI failing + critical code issue)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2791#pullrequestreview-4378086601

## AI Summary
**What it does**: Fixes nvidia-nim chat completion requests sending an empty `model` field (causing 400 "model field is required" errors from the API). Adds a factory guard that bails early when no model can be resolved, and classifies the resulting error body as user-configuration state to suppress Sentry noise from in-flight requests.
**Breaking risk**: Medium — the guard is placed in a shared factory path (`make_cloud_provider_by_slug`) and fires before the openhuman slug's own backend routing, breaking normal openhuman usage.
**Security risk**: Zero — no auth changes, no user input surfaces, no secrets.
**Bottom line**: Not safe to merge — critical regression in factory.rs breaks openhuman slug routing (confirmed by CI failure).
