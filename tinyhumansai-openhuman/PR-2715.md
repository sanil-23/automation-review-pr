# PR #2715 — feat(tool_policy): diagnostics RPC and Developer Options panel
- **Author**: @MrMrVlad
- **Branch**: feat/2136-tool-policy-diagnostics → main
- **Created**: 2026-05-26
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2715
- **Status**: clean
- **Last reviewed commit**: 896cf15806f63be0d2a202733f5ecd60b484a765
- **Last review date**: 2026-05-29T00:00:00Z

## Review History

### Review 1 — 2026-05-28T18:28:44Z
**Type**: Fresh
**Commit**: 896cf15806f63be0d2a202733f5ecd60b484a765
**Summary**: Extends `tool_registry.diagnostics` RPC with policy posture, MCP allowlist summaries, write-audit health, and a redacted in-memory ring buffer of recent policy denials. Adds a Developer Options → Diagnostics settings panel with full i18n coverage across 13 locales. Also bundles scaffolding types in `generated.rs` for a future admission-control feature.
**Gates**: CI FAILING (Rust Core Tests + Quality, Rust Core Coverage) | Conflicts pass | No prior graycyrus feedback
**Areas changed**: Rust core (tool_registry, agent harness), Frontend (settings panel, i18n), Tests
**Red flags**: CI failing; dead code bundled from out-of-scope future feature
**Linked issues**: Closes #2136
**PR-Issue alignment**: Issue asks for RPC reporting visible tools, policy mode, recent denials, and a UI surface — all present. Conformance reporting (runtime contract hash, copyable bundle) deferred to follow-up PR per PR description — acceptable.
**CodeRabbit dedup**: CR flagged regex over-escaping in test, missing i18n localization, raw reason not redacted, non-deterministic tests, i18n chunk parity, missing #[cfg(test)] on ops_test module — all marked addressed in commits. Not repeated.
**Resolution actions**: None (first review)
**Surrounding code patterns checked**: denials.rs (ring buffer, redact_sensitive, truncate_reason), ops.rs (mcp_write_audit_health, posture_from_config), generated.rs (admission structs, risk enum)
**Dependency audit**: N/A — no new external dependencies
**Test coverage**: Unit tests added in denials.rs (3 tests: bounds, empty-tool guard, redaction). ops_test.rs extended with posture/audit assertions. Frontend panel test added. Coverage CI job failing — can't confirm 80% gate.
**Impact scan**: ToolPolicyDiagnostics struct extends with new fields (additive). GeneratedToolDefinition gets optional new fields with None defaults in ::new() — all callers backwards compatible.
**Findings**:
- [major] src/openhuman/tools/generated.rs — `GeneratedToolAdmissionConfig`, `GeneratedToolAdmissionReport`, and `is_external_effect()` have no callers in this PR. If crate treats `dead_code` warnings as errors, these are likely causing Rust Core Tests + Quality CI failure. Should move to follow-up PR or be explicitly `#[allow(dead_code)]` with tracking comment.
- [minor] src/openhuman/tool_registry/ops.rs — `mcp_write_audit_health` hardcodes `enabled: true` in both Ok and Err arms regardless of config. Semantically incorrect if the audit can be disabled.
**Action taken**: COMMENT (CI failing) — tagged @MrMrVlad with both issues + CI hold
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2715#pullrequestreview-4383638654

## AI Summary
- **What it does**: Adds a `tool_registry.diagnostics` RPC extension with policy posture, MCP allowlist summaries, write-audit health, and a redacted in-memory ring buffer of recent policy denials. Surfaces all of this in a new Developer Options → Diagnostics settings panel with proper i18n coverage. Also bundles future admission-control scaffolding in `generated.rs`.
- **Breaking risk**: Low. New fields on `ToolPolicyDiagnostics` are additive. `GeneratedToolDefinition` new optional fields default to `None` in `::new()`.
- **Security risk**: Low. Denial records are redacted via `redact_sensitive()` (catches Bearer, sk-, ghp_, -----BEGIN markers) and truncated to 240 chars before storage. No secrets surfaced in RPC payload.
- **Bottom line**: Safe to merge once CI passes — the dead code in `generated.rs` needs to be addressed first.
