# PR #2851 — fix(subconscious): preserve anyhow chain + retry DDL with busy_timeout on SQLITE_BUSY
- **Author**: @oxoxDev
- **Branch**: fix/sentry-A-subconscious-ddl-resilience → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2851
- **Status**: clean
- **Last reviewed commit**: a0b6de7a1fbefe3ac0179e5d519585c7fcbd9aff
- **Last review date**: 2026-05-28T13:35:46Z

## Review History

### Review 1 — 2026-05-28T13:35:46Z
**Type**: Fresh
**Commit**: a0b6de7a1fbefe3ac0179e5d519585c7fcbd9aff
**Summary**: Two-commit fix for Sentry issue TAURI-RUST-A (~1,383 events/24h). Commit 1 swaps 13 `.map_err(|e| e.to_string())` → `format!("{e:#}")` in schemas.rs to preserve the anyhow error chain at the RPC boundary. Commit 2 refactors `with_connection` in store.rs to add per-connection `busy_timeout(5s)` + 3-attempt exponential backoff retry (100/300/900ms) gated by an `is_sqlite_busy` classifier. 9 new tests added across schemas_tests.rs and store_tests.rs.
**Gates**: CI failing | Conflicts pass | No prior graycyrus feedback
**Areas changed**: Rust core — subconscious module (store.rs, schemas.rs, tests)
**Red flags**: None
**Linked issues**: None (Sentry-only, no GitHub issue)
**PR-Issue alignment**: N/A — Sentry-driven fix, no linked GitHub issue
**CodeRabbit dedup**: CodeRabbit posted no inline comments (empty). Their summary covered error message improvements and retry logic — no overlap with my findings.
**Resolution actions**: None
**Surrounding code patterns checked**: None needed — changes are self-contained within the subconscious store module
**Dependency audit**: No new dependencies
**Test coverage**: 9 new tests: 2 chain-formatter guards (schemas_tests), 6 classifier guards + 1 end-to-end lock-race test (store_tests). Coverage gate passes per CI.
**Impact scan**: `with_connection` signature unchanged — callers unaffected. RPC error strings now contain more detail (full chain vs. outer only) — intended behavior change, not a breaking contract change.
**Findings**:
- [minor] src/openhuman/subconscious/store.rs — `std::thread::sleep` in `open_and_initialize_with_retry` runs on the calling thread. If RPC handlers invoke `with_connection` from an async executor thread (not `spawn_blocking`), sleeping up to 1.3s in the retry path blocks a runtime thread. Matches prior art in `whatsapp_data::sqlite_retry` / `memory_queue::worker` — likely acceptable if those domains use the same pattern. Worth confirming call sites use `spawn_blocking`.

**AI Summary**:
- **What it does**: Fixes two related issues causing Sentry TAURI-RUST-A: (1) error chain flattening at the RPC boundary that stripped the rusqlite root cause from Sentry events, and (2) zero busy_timeout on SQLite connections causing immediate SQLITE_BUSY failures under concurrent RPC load. The fix improves both observability and resilience.
- **Breaking risk**: Zero. `with_connection` public signature unchanged. Error strings become more verbose — intended.
- **Security risk**: Zero. No secrets/PII path. Error strings gaining more detail is the explicit goal.
- **Bottom line**: Safe to merge — CI failures are pre-existing (checklist formatting nit + known discord_integration SIGKILL flake).

**Action taken**: COMMENT — code is clean, CI not fully green (PR Submission Checklist: 3 N/A items marked `[ ]` instead of `[x]`; Rust Core Tests: known discord_integration SIGKILL). Will approve once CI is green.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2851#pullrequestreview-4381389413
