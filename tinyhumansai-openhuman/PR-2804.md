# PR #2804 — fix(yuanbao): correct AuthBind plugin_version / bot_version mapping
- **Author**: @lrt4836
- **Branch**: fix/yuanbao-auth-bind-versions → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2804
- **Status**: changes-requested
- **Last reviewed commit**: ab178cee28cb1a27ce2378d138a22acf81fa8fd6
- **Last review date**: 2026-05-28T05:18:58Z

## Review History

### Review 1 — 2026-05-28T05:18:58Z
**Type**: Fresh
**Commit**: ab178cee28cb1a27ce2378d138a22acf81fa8fd6
**Summary**: Fixes version mapping inversion in Yuanbao provider's AuthBind protocol. Swaps `CARGO_PKG_VERSION` and `cfg.bot_version` to match `yuanbao-openclaw-plugin`'s `sendAuthBind` semantics, strips legacy `openhuman/` prefix from config, and bumps `OPENHUMAN_INSTANCE_ID` from 16 to 20. 5 Rust files changed, 23 additions / 9 deletions.
**Gates**: CI FAILING (Rust Quality fmt+clippy, TypeScript type check, PR Submission Checklist) | Conflicts: N/A | Unresolved feedback: CodeRabbit changes requested
**Areas changed**: Rust core (yuanbao provider — config, connection, proto, proto_constants, sign)
**Red flags**: CI failing on fmt+clippy; version constant mismatch (0.1.1 vs 0.1.0 in PR description/tests)
**Linked issues**: None
**PR-Issue alignment**: Bug fix, no linked issue (acceptable for targeted protocol fix)
**CodeRabbit dedup**: Skipped — version 0.1.1 vs 0.1.0 mismatch on DEFAULT_PLUGIN_VERSION (CodeRabbit flagged)
**Resolution actions**: None
**Surrounding code patterns checked**: None needed — changes are self-contained
**Dependency audit**: N/A (no new dependencies)
**Test coverage**: proto.rs auth_bind_smoke updated to match new arg order; coverage gate passes
**Impact scan**: encode_auth_bind args swapped — test updated; sign.rs headers updated consistently

## AI Summary
**What it does**: Corrects an inverted mapping in the Yuanbao AuthBind handshake where `CARGO_PKG_VERSION` (the OpenHuman framework version) was incorrectly sent as `app_version` (the plugin identifier slot) and `cfg.bot_version` was sent as `bot_version`. The PR swaps them to match the upstream `yuanbao-openclaw-plugin` contract. Also updates OPENHUMAN_INSTANCE_ID from 16 to 20.
**Breaking risk**: Low — corrects wrong protocol behavior; existing configs using `bot_version` TOML field will see their value rerouted to `app_version` (intentional, but no changelog note).
**Security risk**: Zero — no auth bypass, credential exposure, or injection surface changes.
**Bottom line**: Safe to merge once CI passes and version string inconsistency (0.1.1 vs 0.1.0) is resolved.

**Findings**:
- [minor] config.rs:54 — `bot_version` TOML field now maps to `app_version` (plugin_version) in the protocol; existing deployments will silently see their configured value used as the plugin identifier. Field name is now semantically misleading. Needs changelog note or deprecation comment.

**Action taken**: COMMENT — CI failing (Rust fmt+clippy, TypeScript type check, PR checklist); flagged version inconsistency (CodeRabbit) and bot_version field naming issue
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2804#pullrequestreview-4378082150
