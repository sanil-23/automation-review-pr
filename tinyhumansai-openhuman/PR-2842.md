# PR #2842 — perf(prompt_injection): batch detection rules via RegexSet, drop dead compact scan
- **Author**: @mysma-9403
- **Branch**: feature/batch-prompt-injection-regexset → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2842
- **Status**: clean
- **Last reviewed commit**: 71aa087b5ce68bb8650f701098ab5775eae2816b
- **Last review date**: 2026-05-28T12:16:47Z

## AI Summary
- **What it does**: Replaces 18 independent `Regex::is_match` calls with 3 `RegexSet::matches` DFA passes (one per normalized variant: lowered, collapsed, compact) in the prompt injection detector hot path. Also removes a redundant pre-scan for `had_zwsp` by detecting it inline during the existing normalization loop. Detection patterns, scores, and verdicts are unchanged.
- **Breaking risk**: Low. `DetectionRule` struct is private; output (verdicts, scores, reason codes) is identical for all inputs.
- **Security risk**: Low. This is the security module itself. Code correctly preserves all three normalized variant scans — the PR description falsely claimed compact was dropped, but the code (correctly) keeps it after CodeRabbit flagged the regression in 71aa087.
- **Bottom line**: Safe to merge — behavior-preserving performance refactor with solid test coverage.

## Review History

### Review 1 — 2026-05-28T12:16:47Z
**Type**: Fresh
**Commit**: 71aa087b5ce68bb8650f701098ab5775eae2816b
**Summary**: Pure performance refactor of `src/openhuman/prompt_injection/detector.rs`. Replaces 18 independent `Regex::is_match` calls per chat turn with 3 `RegexSet::matches` DFA passes. Removes a redundant `lowered.chars().any(is_obfuscation_char)` pre-scan by setting `had_zwsp` inline in the normalization loop. Adds 2 regression tests in `tests.rs`.
**Gates**: CI pending | Conflicts pass | Unresolved feedback pass (CodeRabbit CHANGES_REQUESTED addressed in 71aa087, no human reviewers)
**Areas changed**: Rust core (`src/openhuman/prompt_injection/`)
**Red flags**: Stale PR description claims compact variant was dropped — code correctly keeps it; description not updated after 71aa087
**Linked issues**: None
**PR-Issue alignment**: N/A (no linked issue; perf optimization with clear motivation)
**CodeRabbit dedup**: CodeRabbit CHANGES_REQUESTED flagged missing compact scan — addressed in 71aa087, that finding is resolved and not repeated
**Resolution actions**: CodeRabbit compact-scan issue resolved by author in 71aa087
**Surrounding code patterns checked**: None needed — changes are self-contained within the detector module
**Dependency audit**: N/A (uses existing `regex` crate `RegexSet`, no new deps)
**Test coverage**: Good — 2 new regression tests: `each_detection_rule_is_individually_reachable` (guards index alignment) and `compact_variant_catches_spacing_obfuscated_single_token_rules` (guards compact scan)
**Impact scan**: `DetectionRule` struct is private; `DETECTION_RULES` changed from `Lazy<Vec<_>>` to `&[_]`, callers still use `.iter()` — no breaking change; verdict/score/reason output identical
**Findings**:
- [minor] PR description body says "Drop the compact variant from the rule-scan loop" and "Why this shape" table marks keeping compact as "Rejected" — stale after 71aa087 kept it. Code and inline comments are correct; PR body misleads git history readers.
**Action taken**: COMMENT — CI pending, code clean, will approve once CI green
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2842#pullrequestreview-4380824619
