# PR #2748 — Add Codex OAuth provider support
- **Author**: @junjunjunbong
- **Branch**: `codex/add-codex-oauth-provider` → `main`
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2748
- **Status**: clean
- **Last reviewed commit**: 0db55dc6990c53469e3c6ee965f02ebd4621e922
- **Last review date**: 2026-05-27T11:46:22Z

## Review History

### Review 1 — 2026-05-27T11:46:22Z
**Type**: Fresh
**Commit**: 0db55dc6990c53469e3c6ee965f02ebd4621e922
**Summary**: Adds Codex/ChatGPT OAuth as a sign-in option for OpenAI provider. Users can import existing Codex CLI credentials (`~/.codex/auth.json`) instead of using API keys. Implements model catalog normalization (accepts OpenAI `data` and Codex `models` response shapes), structured error handling with localized messages, and safe transaction sequencing (clears API key only after successful persistence).
**Gates**: CI pending (coverage + E2E tests still running); lint/format/type checks all pass; tests pass (Rust core + frontend unit)
**Areas changed**: Frontend (AIPanel, aiSettingsApi), Rust core (openai_oauth flow/store, provider factory, model catalog parser), i18n (all locales)
**Red flags**: None
**Linked issues**: N/A (user-reported local regression, no GitHub issue)
**PR-Issue alignment**: N/A
**CodeRabbit dedup**: All CodeRabbit findings addressed:
  - Codex key clear moved after `persist()` succeeds (safe transaction order)
  - `codexAuthError` state cleared on successful OpenAI connect
  - OAuth validation errors translated via error codes, not English strings
  - Windows home-dir fallback implemented (HOME, USERPROFILE, HOMEDRIVE+HOMEPATH)
  - Structured diagnostics for Codex CLI import (grep-friendly logs, no secrets)
  - OAuth metadata tolerant when API-key routing is available
  - Korean i18n duplicates removed from ko-1.ts, kept canonical in ko-4.ts
**Resolution actions**: None (all prior findings resolved by author)
**Surrounding code patterns checked**: Checked openai_oauth store (token persistence), provider factory (credential lookup), AIPanel connectProvider flow (state ordering), model catalog normalization
**Dependency audit**: N/A
**Test coverage**: New tests cover happy path (Codex CLI auth import succeeds) and failure paths (missing auth URL, missing callback URL). Error localization verified.
**Impact scan**: Low breaking risk — credential mode `codex_oauth` is opt-in. Model catalog parsing backward-compatible. No public API changes, no DB schema changes. Security risk low — file I/O validated, no secrets logged, tokens via encrypted profiles.
**Findings**: None (code clean)
**Action taken**: Posted COMMENT review to GitHub (approval deferred until CI fully passes)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2748#pullrequestreview-4372090470

## AI Summary

**What it does**: Adds Codex OAuth provider support to OpenAI — users can now authenticate via ChatGPT Codex by importing existing Codex CLI credentials (`~/.codex/auth.json`) instead of using API keys. The implementation adds new OAuth RPC operations, model catalog normalization (supports both OpenAI `data` and Codex `models` response formats), and structured error handling with localized messages.

**Breaking risk**: Low. New credential mode `codex_oauth` is opt-in. Model catalog parsing is backward-compatible (accepts both response shapes). No public API changes, no DB schema changes.

**Security risk**: Low. File I/O reads from local `~/.codex/auth.json`, validates on read, no secrets logged, tokens stored via existing encrypted auth profiles. Windows home-dir fallback is correctly implemented. OAuth metadata is tolerant of broken profiles when API-key fallback exists.

**Bottom line**: Safe to merge once CI passes. Code is clean, well-tested, all CodeRabbit findings were addressed.
