# Intelligence Context — PR #2070

## PR Metadata
- **Title**: feat(i18n): translate ~3,900 strings across 9 locales + add Italian
- **Author**: @senamakel
- **Branch**: feat/lang-coverage → main
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2070
- **Review type**: Fresh
- **Latest commit**: b663fa6edfc1d7831c6cc066a520ede10fab128f
- **Commit count**: 10
- **Diff size**: 62 files, +4478 -1710 lines (~10,615 diff lines)
- **Labels**: feature, working

## PR Description
New `pnpm i18n:check` script + Vitest guard surface missing / extra / drifted / placeholder keys across every locale, so future translation drift hard-fails CI. Translated ~2,180 placeholder entries across the 9 existing non-English locales (en-identical → native). Added Italian (it) as a 10th supported locale — 1,678 translated entries on first cut, wired into `Locale` union, `I18nContext`, language picker, browser-locale autodetect, and both coverage scripts. Fixed an `en.ts` ↔ `en-N.ts` drift (two keys present only in the runtime aggregate) and made the script hard-fail on future drift.

Per-locale placeholder reduction (from PR description):
| Locale | Before | After |
|---|---:|---:|
| hi | 323 | 34 |
| ru | 273 | 35 |
| fr | 269 | 101 |
| id | 259 | 92 |
| es | 249 | 65 |
| pt | 243 | 75 |
| bn | 201 | 37 |
| ar | 195 | 33 |
| zh-CN | 171 | 29 |
| it (new) | — | 85 |

Validation reported:
- `pnpm --filter openhuman-app format:check` ✓
- `pnpm typecheck` ✓
- Focused tests: `pnpm exec vitest run --config test/vitest.config.ts src/lib/i18n/` (7/7 pass)
- No Rust or Tauri changes

## Red Flags
- **No linked issue** — acceptable for this type of i18n chore/feature, but noted. PR description acknowledges "N/A: no linked issue."
- **Very large diff (4478+/1710-)** — however, the vast majority is mechanical translation string changes across 50 locale chunk files. The actual logic changes are limited to ~5-6 files.
- **10 commits** — iterative development with CodeRabbit feedback addressed mid-stream. Commits are well-organized: fix(start.sh), chore(i18n) coverage script, feat(i18n) translations, feat(i18n) Italian, chore prettier, ci jobs, test refactor, fix CodeRabbit feedback, ci restructure, chore tsx dep.
- **Unrelated change bundled**: `scripts/shortcuts/work/start.sh` — awk variable passing fix (BSD awk compatibility). Minor scope creep but reasonable to bundle.

## Linked Issues + Acceptance Criteria
None — no linked issue. Acceptable for translation/tooling work.

## PR-Issue Alignment (three-way verification)
N/A — no linked issue. PR description is detailed and matches the actual changes.

## Prior Review Context (continuation only)
N/A — Fresh review.

## Review Feedback + Resolution Candidates

### CodeRabbit Reviews
CodeRabbit posted **CHANGES_REQUESTED** on commit 80a77368 with 10 actionable comments. All 10 have been marked "✅ Addressed in commit 350b7ff" and all threads are **resolved** and **outdated** (code changed since).

A second CodeRabbit review (COMMENTED) on commit 44b45422 had 1 nitpick about hardcoded `CHUNK_COUNT`/`ALL_LOCALES` in `scripts/i18n-coverage.ts` — this is **unresolved** (see dedup below).

The first CHANGES_REQUESTED review was later **DISMISSED** (review ID 4309030030) after fixes were pushed.

### All review threads (from GraphQL):
All 10 inline threads are **resolved** and **outdated**:
1. `hi-4.ts` — broken Hindi grapheme spacing → ✅ fixed in 350b7ff
2. `hi-4.ts` — `permScreen` wrong concept → ✅ fixed in 350b7ff
3. `it-4.ts` — `rewards.community.locked` inverted meaning → ✅ fixed in 350b7ff
4. `it-4.ts` — malformed UI labels (`) :` and `:`) → ✅ fixed in 350b7ff
5. `it-5.ts` — `paused` state label inverted → ✅ fixed in 350b7ff
6. `it-5.ts` — `show*` labels say "hide" → ✅ fixed in 350b7ff
7. `it-5.ts` — webhook tunnel labels reversed → ✅ fixed in 350b7ff
8. `ru-5.ts` — stray leading "s" in Russian string → ✅ fixed in 350b7ff
9. `apply-i18n-translations.ts` — `jsString()` missing control char escaping (CRITICAL) → ✅ fixed in 350b7ff
10. `i18n-coverage.ts` — `--out`/`--locale` missing arg validation → ✅ fixed in 350b7ff

### Unresolved CodeRabbit nitpick (from second review):
- `scripts/i18n-coverage.ts:34-49` — `CHUNK_COUNT` and `ALL_LOCALES` hardcoded, creating second source of truth. Suggests deriving from filesystem/shared metadata. **Not addressed yet.** This is a valid architectural concern but a nitpick — the CI test file (`coverage.test.ts`) also hardcodes these same constants.

### No graycyrus reviews exist yet — this is our first review.

## Changed Files
| File | Change type |
|------|-------------|
| `.github/workflows/test-reusable.yml` | Modified — added i18n-coverage CI job, Prettier quote style fixes |
| `app/src/components/LanguageSelect.tsx` | Modified — added Italian option |
| `app/src/lib/i18n/I18nContext.tsx` | Modified — import + register Italian |
| `app/src/lib/i18n/__tests__/I18nContext.test.tsx` | Modified — updated test for missing key fallback |
| `app/src/lib/i18n/__tests__/coverage.test.ts` | **New** — Vitest i18n coverage guard |
| `app/src/lib/i18n/chunks/ar-{1..5}.ts` | Modified — Arabic translations |
| `app/src/lib/i18n/chunks/bn-{1..5}.ts` | Modified — Bengali translations |
| `app/src/lib/i18n/chunks/en-3.ts` | Modified — added 2 missing keys |
| `app/src/lib/i18n/chunks/es-{1..5}.ts` | Modified — Spanish translations |
| `app/src/lib/i18n/chunks/fr-{1..5}.ts` | Modified — French translations |
| `app/src/lib/i18n/chunks/hi-{1..5}.ts` | Modified — Hindi translations |
| `app/src/lib/i18n/chunks/id-{1..5}.ts` | Modified — Indonesian translations |
| `app/src/lib/i18n/chunks/it-{1..5}.ts` | **New** — Italian chunk files (5 files) |
| `app/src/lib/i18n/chunks/pt-{1..5}.ts` | Modified — Portuguese translations |
| `app/src/lib/i18n/chunks/ru-{1..5}.ts` | Modified — Russian translations |
| `app/src/lib/i18n/chunks/zh-CN-{1..5}.ts` | Modified — Simplified Chinese translations |
| `app/src/lib/i18n/it.ts` | **New** — Italian locale aggregator |
| `app/src/lib/i18n/types.ts` | Modified — added `'it'` to Locale union |
| `app/src/store/localeSlice.ts` | Modified — added `['it', 'it']` to browser prefix map |
| `package.json` | Modified — added `i18n:check`, `i18n:dump` scripts + `tsx` devDep |
| `pnpm-lock.yaml` | Modified — lockfile update for tsx |
| `scripts/apply-i18n-translations.ts` | **New** — chunk regenerator script |
| `scripts/i18n-coverage.ts` | **New** — coverage analyzer script |
| `scripts/shortcuts/work/start.sh` | Modified — BSD awk compatibility fix |

## Classification + Checklist

### Areas detected: Frontend, CI/workflows, Scripts/Tooling

**Frontend files changed:**
- [x] No dynamic imports — `coverage.test.ts` uses `import.meta.glob` with `{ eager: true }` (static at build time, explicitly noted as CLAUDE.md compliant)
- [x] Config read correctly — no direct `import.meta.env` usage
- [x] State in Redux — `localeSlice.ts` change follows existing pattern
- [x] Tauri access — not applicable (no Tauri API calls added)
- [ ] Verify Italian locale wiring complete: types.ts, I18nContext.tsx, LanguageSelect.tsx, localeSlice.ts — all four touched

**CI/workflows changed:**
- [ ] New `i18n-coverage` job added to `test-reusable.yml` — verify it runs under correct condition (`inputs.run_unit`)
- [ ] Uses same CI container image (`ghcr.io/tinyhumansai/openhuman_ci:rust-1.93.0`) — seems heavy for a pure JS/TS job (no Rust needed)
- [ ] Prettier quote style changes (single → double quotes on YAML values) — cosmetic, no functional change

**Scripts/Tooling:**
- [ ] `scripts/apply-i18n-translations.ts` — new file, not run in CI, development tool only
- [ ] `scripts/i18n-coverage.ts` — new file, run via `pnpm i18n:check` in CI
- [ ] Both scripts use `tsx` runtime — now declared as devDependency
- [ ] `jsString()` escaping — CodeRabbit flagged and fixed (control chars, U+2028/U+2029)

**Tests:**
- [ ] `coverage.test.ts` — new Vitest guard covers: en aggregate ↔ chunks parity, missing chunk files, missing/extra/drifted keys per locale
- [ ] `I18nContext.test.tsx` — updated to test missing key fallback (raw key returned instead of English fallback for truly unknown keys)

## Surrounding Code Patterns

### app/src/lib/i18n/
**Files read**: `en.ts`, `fr.ts`, `zh-CN.ts`, `chunks/en-1.ts`, `chunks/en-4.ts`, `types.ts`
- **Naming**: Locale aggregators are `<locale>.ts`, chunks are `chunks/<locale>-N.ts` (N=1-5). Variable names: `const fr: TranslationMap = { ...fr1, ...fr2, ...fr3, ...fr4, ...fr5 }`
- **Error handling**: No error handling in locale files — pure data
- **Imports**: Each chunk imports `TranslationMap` from `'../types'`. Aggregators import 5 chunks + type.
- **Logging**: N/A
- **Tests**: `I18nContext.test.tsx` tests locale rendering + fallback behavior. New `coverage.test.ts` tests key parity.
- **Pattern note**: English (`en.ts`) is a single monolithic file, NOT chunked like other locales. Other locales use 5 chunks for code-splitting. The coverage test correctly loads chunks via `import.meta.glob` with `{ eager: true }` to avoid dynamic imports.

### app/src/store/
**Files read**: `localeSlice.ts` (in diff)
- **Pattern**: `PREFIX_TO_LOCALE` array maps browser locale prefixes to `Locale` values. Order matters — `'en'` is last (fallback). Italian `['it', 'it']` correctly added before `['en', 'en']`.

## CodeRabbit Dedup

CodeRabbit already flagged (all resolved via commit 350b7ff):
- `app/src/lib/i18n/chunks/hi-4.ts:91-96` — broken Hindi grapheme spacing (actionable, fixed)
- `app/src/lib/i18n/chunks/hi-4.ts:112` — `permScreen` wrong concept (actionable, fixed)
- `app/src/lib/i18n/chunks/it-4.ts:202` — `rewards.community.locked` inverted meaning (actionable, fixed)
- `app/src/lib/i18n/chunks/it-4.ts:293-294` — malformed UI labels (actionable, fixed)
- `app/src/lib/i18n/chunks/it-5.ts:45` — `paused` state label inverted (actionable, fixed)
- `app/src/lib/i18n/chunks/it-5.ts:163-164` — `show*` labels say "hide" (actionable, fixed)
- `app/src/lib/i18n/chunks/it-5.ts:377-378` — webhook tunnel labels reversed (actionable, fixed)
- `app/src/lib/i18n/chunks/ru-5.ts:294-295` — stray "s" in Russian string (actionable, fixed)
- `scripts/apply-i18n-translations.ts:48-50` — `jsString()` missing control char escaping (CRITICAL, fixed)
- `scripts/i18n-coverage.ts:69-72` — missing arg validation for `--out`/`--locale` (actionable, fixed)

**Unresolved nitpick (do NOT duplicate):**
- `scripts/i18n-coverage.ts:34-49` — `CHUNK_COUNT` and `ALL_LOCALES` hardcoded as second source of truth (nitpick, not addressed)

## Dependency Audit

### New JS dependencies
- **`tsx` ^4.20.3** — added as root `devDependency`. This is a widely-used TypeScript executor (esbuild-based). High npm downloads, MIT license. Previously used transitively; now declared directly. Reasonable addition for the `i18n:check` and `i18n:dump` scripts.

No Cargo.toml changes. No production dependencies added.

## Test Coverage

**New logic with tests:**
- `scripts/i18n-coverage.ts` — new 457-line coverage analyzer. Tested indirectly via `coverage.test.ts` (which reimplements some of the same logic in-process for Vitest). The script itself is a CLI tool not directly unit-tested, but its core logic (missing/extra/drift detection) is validated by the Vitest guard.
- `scripts/apply-i18n-translations.ts` — new 195-line chunk regenerator. **No tests.** This is a development-only tool, not run in CI. Acceptable for a codegen script.

**Modified logic with updated tests:**
- `I18nContext.test.tsx` — updated to handle the fact that `bootCheck.quit` is now translated (Indonesian), and tests unknown key fallback.

**Coverage gate:**
- The PR adds `coverage.test.ts` which IS the coverage gate for i18n. It checks all locales for missing/extra/drifted keys.
- No behavioral logic changed — this is pure string data + tooling. The 80% coverage gate is not meaningfully applicable to translation data tables.

## Impact Scan

### Changed exports/types:
- **`Locale` type** (`types.ts`) — union extended with `'it'`. All consumers updated:
  - `I18nContext.tsx` — imports and registers Italian ✓
  - `LanguageSelect.tsx` — adds Italian option ✓
  - `localeSlice.ts` — adds browser prefix mapping ✓
  - `scripts/i18n-coverage.ts` — includes `'it'` in `ALL_LOCALES` ✓
  - `scripts/apply-i18n-translations.ts` — includes Italian in `LOCALE_HEADERS` ✓

- **`en-3.ts`** — two new keys added (`accounts.respondQueue.hide`, `accounts.respondQueue.show`). These are not new exports per se — they were already in `en.ts` aggregate but missing from chunks. All locale chunks should now include them (coverage test enforces this).

No RPC method changes. No Redux state shape changes (locale union is a string literal, persisted `locale.current` handles arbitrary `Locale` values). No function signature changes in exported modules.

## Full Diff

The full diff is 10,615 lines. The vast majority (~95%) is mechanical translation string replacements across 50 locale chunk files. Key structural diffs are included inline above.

The full diff is saved at: /Users/cyrus/.claude/projects/-Users-cyrus-Desktop-automation-review-pr/e63a7fb8-54b3-4404-b81c-668a3f0997b0/tool-results/b3jjxa32e.txt

### Key non-translation diffs (structural changes):

#### .github/workflows/test-reusable.yml
- Added `i18n-coverage` job: checkout → cache pnpm → install → `pnpm i18n:check`
- Uses Rust CI container (overkill for pure TS but matches existing job pattern)
- Minor Prettier quote style changes (single → double on YAML values)

#### app/src/lib/i18n/__tests__/coverage.test.ts (NEW)
```typescript
// 97 lines — Vitest guard for i18n coverage
// Uses import.meta.glob with { eager: true } (static, no dynamic imports)
// Tests: en aggregate ↔ chunks parity, missing chunk files, missing/extra/drifted keys per locale
const CHUNK_COUNT = 5;
const LOCALES = ['zh-CN', 'hi', 'es', 'ar', 'fr', 'bn', 'pt', 'ru', 'id', 'it'] as const;
const chunkModules = import.meta.glob<ChunkModule>('../chunks/*.ts', { eager: true });
```

#### app/src/lib/i18n/it.ts (NEW)
```typescript
// Standard locale aggregator following fr.ts / zh-CN.ts pattern exactly
import it1 from './chunks/it-1';
// ...
const it: TranslationMap = { ...it1, ...it2, ...it3, ...it4, ...it5 };
export default it;
```

#### app/src/lib/i18n/types.ts
```diff
-export type Locale = 'en' | 'zh-CN' | 'hi' | 'es' | 'ar' | 'fr' | 'bn' | 'pt' | 'ru' | 'id';
+export type Locale = 'en' | 'zh-CN' | 'hi' | 'es' | 'ar' | 'fr' | 'bn' | 'pt' | 'ru' | 'id' | 'it';
```

#### app/src/store/localeSlice.ts
```diff
+  ['it', 'it'],
   ['en', 'en'],
```

#### package.json
```diff
+    "i18n:check": "tsx scripts/i18n-coverage.ts",
+    "i18n:dump": "tsx scripts/i18n-coverage.ts --no-unused --out tmp/i18n-coverage"
+    "tsx": "^4.20.3",  // new devDependency
```

#### scripts/apply-i18n-translations.ts (NEW — 195 lines)
- Chunk regenerator: reads JSON translation input, merges with existing chunks, writes locale-N.ts files
- `jsString()` properly escapes backslash, single quote, \n, \r, \t, C0 controls, U+2028, U+2029
- Development tool only, not run in CI

#### scripts/i18n-coverage.ts (NEW — 457 lines)
- Coverage analyzer: loads all chunk files, diffs against English, reports missing/extra/drift/placeholder/unused keys
- CLI with `--json`, `--locale`, `--no-unused`, `--strict-unused`, `--out` options
- Validates CLI args (--out and --locale require values, per CodeRabbit fix)
- Uses `t('...')` regex scan of `app/src/` for unused key detection
- Hardcodes `CHUNK_COUNT = 5` and `ALL_LOCALES` (CodeRabbit nitpick — second source of truth)

#### scripts/shortcuts/work/start.sh
- BSD awk compatibility fix: passes variables via ENVIRON[] instead of `-v var=value` (which breaks on literal newlines in BSD awk)

#### app/src/lib/i18n/chunks/en-3.ts
```diff
+  'accounts.respondQueue.hide': 'Hide Respond Queue',
+  'accounts.respondQueue.show': 'Show Respond Queue',
```
Two keys that existed in `en.ts` aggregate but were missing from chunks — drift fix.
