# PR #2652 — fix(channels): surface structured rate-limit metadata on chat_error
- **Author**: @CodeGhost21
- **Branch**: fix/2606-structured-rate-limit-metadata → main
- **Created**: 2026-05-25
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2652
- **Status**: approved
- **Last reviewed commit**: ffac3133033d3c641a1c19b4309e2cfd9c09734c
- **Last review date**: 2026-05-27T13:07:51Z

## AI Summary
- **What it does**: Promotes rate-limit error metadata from message-text-only to typed wire fields on `chat_error` WebChannelEvent. Adds five additive optional fields (`error_source`, `error_retryable`, `error_retry_after_ms`, `error_provider`, `error_fallback_available`) and refactors `classify_inference_error` from returning a `(&'static str, String)` tuple to a `ClassifiedError` struct. Distinguishes upstream provider 429s, OpenHuman budget caps, agent loop limits, billing exhaustion, transport failures, and config errors.
- **Breaking risk**: Zero. All new fields are `Option<…>` with `skip_serializing_if = "Option::is_none"` — older FE clients see identical JSON shape.
- **Security risk**: Zero. `extract_provider_name` allow-lists to `[a-zA-Z0-9_-]`. No new IO or external dependencies.
- **Bottom line**: Safe to merge.

## Review History

### Review 1 — 2026-05-27T13:07:51Z
**Type**: Fresh
**Commit**: ffac3133033d3c641a1c19b4309e2cfd9c09734c
**Summary**: Adds `ClassifiedError` struct and refactors `classify_inference_error` to populate five new optional `WebChannelEvent` wire fields. Covers 10 error branches. 13 new tests (11 classifier unit, 2 wire-shape/JSON-omit). Also replaces flaky `TestForcedRunChatTaskErrorGuard` with a proper `TokioMutex` for test isolation.
**Gates**: CI pass | No conflicts | No prior graycyrus feedback
**Areas changed**: Rust core — `src/core/socketio.rs`, `src/openhuman/channels/providers/web.rs`, `src/openhuman/channels/providers/presentation.rs`, `src/openhuman/channels/proactive.rs`, `src/openhuman/channels/providers/web_tests.rs`
**Red flags**: None
**Linked issues**: Closes #2606
**PR-Issue alignment**: All 7 acceptance criteria from #2606 are met. Structured fields surfaced: `retryable`, `source`, `provider`, `retry_after_ms`, fallback availability. Limit types distinguished. Retry-After preserved. 13 new tests + coverage gate pass.
**CodeRabbit dedup**: CR flagged two major issues (non-retryable 429 message copy contradiction at web.rs:477; `openhuman_billing` misdirection for provider 402s at web.rs:526) — both marked "Addressed in commit ffac313". Confirmed fixed in current diff. Not repeated.
**Resolution actions**: None (first review)
**Surrounding code patterns checked**: None needed — changes are self-contained within the classifier and event publish path
**Dependency audit**: No new external dependencies introduced (pure string classification, no IO)
**Test coverage**: 13 new tests: classifier unit tests for every new structured field (retry_after_ms, source, provider, retryable, fallback_available), plus wire-shape and None-omit contract tests. CI Coverage Gate (diff-cover ≥ 80%) passed.
**Impact scan**: WebChannelEvent is a wire struct; all new fields are additive `Option<…>` with `skip_serializing_if`. No callers break. Existing `error_type` tokens unchanged.
**Findings**:
- [minor] src/openhuman/channels/providers/web.rs — `is_non_retryable_rate_limit_text`: "not include" hint is broader than the rest. Safe in 429 context but consider tightening to "not included in" or adding a clarifying comment.
- [minor] src/openhuman/channels/providers/web.rs — 402 branch: when `source = "provider"`, user-facing message still says "Please top up to continue." (implies OpenHuman billing). Not worse than pre-PR; FE follow-up should handle `error_source == "provider" && error_type == "budget_exhausted"` explicitly.
**Action taken**: APPROVED
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2652#pullrequestreview-4372553524
