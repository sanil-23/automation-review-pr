# PR #2748 — Add Codex OAuth provider support
- **Author**: @junjunjunbong
- **Branch**: `codex/add-codex-oauth-provider` → `main`
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2748
- **Status**: approved
- **Last reviewed commit**: 2de8ab3722f6c448b2ee12f2f6742c808a88bbf4
- **Last review date**: 2026-05-28T00:00:00Z

## Review History

### Review 1 — 2026-05-27T11:46:22Z
**Type**: Fresh
**Commit**: 0db55dc6990c53469e3c6ee965f02ebd4621e922
**Summary**: Adds Codex/ChatGPT OAuth as a sign-in option for OpenAI provider. Users can import existing Codex CLI credentials (`~/.codex/auth.json`) instead of using API keys. Implements model catalog normalization (accepts OpenAI `data` and Codex `models` response shapes), structured error handling with localized messages, and safe transaction sequencing (clears API key only after successful persistence).
**Gates**: CI pending (coverage + E2E tests still running); lint/format/type checks all pass; tests pass (Rust core + frontend unit)
**Areas changed**: Frontend (AIPanel, aiSettingsApi), Rust core (openai_oauth flow/store, provider factory, model catalog parser), i18n (all locales)
**Red flags**: None
**Linked issues**: N/A (user-reported local regression, no GitHub issue)
**PR-Issue alignment**: N/A
**CodeRabbit dedup**: All CodeRabbit findings addressed:
  - Codex key clear moved after `persist()` succeeds (safe transaction order)
  - `codexAuthError` state cleared on successful OpenAI connect
  - OAuth validation errors translated via error codes, not English strings
  - Windows home-dir fallback implemented (HOME, USERPROFILE, HOMEDRIVE+HOMEPATH)
  - Structured diagnostics for Codex CLI import (grep-friendly logs, no secrets)
  - OAuth metadata tolerant when API-key routing is available
  - Korean i18n duplicates removed from ko-1.ts, kept canonical in ko-4.ts
**Resolution actions**: None (all prior findings resolved by author)
**Surrounding code patterns checked**: Checked openai_oauth store (token persistence), provider factory (credential lookup), AIPanel connectProvider flow (state ordering), model catalog normalization
**Dependency audit**: N/A
**Test coverage**: New tests cover happy path (Codex CLI auth import succeeds) and failure paths (missing auth URL, missing callback URL). Error localization verified.
**Impact scan**: Low breaking risk — credential mode `codex_oauth` is opt-in. Model catalog parsing backward-compatible. No public API changes, no DB schema changes. Security risk low — file I/O validated, no secrets logged, tokens via encrypted profiles.
**Findings**: None (code clean)
**Action taken**: Posted COMMENT review to GitHub (approval deferred until CI fully passes)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2748#pullrequestreview-4372090470

### Review 2 — 2026-05-28T00:00:00Z
**Type**: Continuation
**Commit**: 2de8ab3722f6c448b2ee12f2f6742c808a88bbf4
**Summary**: New commit `fix(inference): route Codex OAuth through responses API` adds `responses_api_primary` flag to route Codex OAuth inference directly through the Responses API (bypassing chat completions). Adds `ResponsesContentPart` type for proper input_text/output_text content differentiation, namespaced JWT claim extraction for account ID, `merge_openai_codex_model_hints` with case-insensitive dedup, and `originator`/user-agent headers for OAuth model listing.
**Gates**: CI fully green (all jobs pass); no conflicts; no unresolved threads
**Areas changed**: Rust core (openai_oauth/store, provider/compatible, compatible_parse, compatible_types, factory, ops, openai_codex)
**Red flags**: None
**Linked issues**: N/A
**PR-Issue alignment**: N/A
**CodeRabbit dedup**: No new CodeRabbit findings on the new commit
**Resolution actions**: None
**Surrounding code patterns checked**: compatible.rs streaming branch, chat_via_responses usage, ResponsesInput/ResponsesContentPart serialization
**Dependency audit**: N/A
**Test coverage**: New tests: nested JWT claim extraction, `responses_api_primary` mock-server routing, model hint dedup, updated Responses prompt content assertions. Coverage gate passed.
**Impact scan**: `responses_api_primary` is opt-in (only set for Codex OAuth). `ResponsesInput.content` type change is internal — no public API break. `merge_openai_codex_model_hints` is additive.
**Findings**:
- [minor] `src/openhuman/inference/provider/compatible.rs` streaming branch — `responses_api_primary` buffers full response and emits as single TextDelta. The Responses API supports SSE; follow-up PR to wire up streaming for this path would improve UX on long responses.
**Action taken**: Posted APPROVE review to GitHub
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2748#pullrequestreview-4375542395

## AI Summary

**What it does**: Adds Codex OAuth provider support to OpenAI — users can now authenticate via ChatGPT Codex by importing existing Codex CLI credentials (`~/.codex/auth.json`) instead of using API keys. Routes Codex OAuth inference through the Responses API with proper content typing, correct JWT claim extraction, model hint injection, and required request headers.

**Breaking risk**: Low. All new behavior is opt-in via `codex_oauth` credential mode and `responses_api_primary` flag. Model catalog parsing is backward-compatible. No public API changes, no DB schema changes.

**Security risk**: Low. File I/O reads from local `~/.codex/auth.json`, validates on read, no secrets logged, tokens stored via existing encrypted auth profiles.

**Bottom line**: Safe to merge.
