# PR #2828 — feat(mcp): implement resources/templates/list as static empty catalog
- **Author**: @obchain
- **Branch**: feat/mcp-resources-templates-list → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2828
- **Status**: approved
- **Last reviewed commit**: 95f25985b4b3639fa7d284674b7161f4fd555468
- **Last review date**: 2026-05-28T11:42:20Z

## Review History

### Review 1 — 2026-05-28T11:42:20Z
**Type**: Fresh
**Commit**: 95f25985b4b3639fa7d284674b7161f4fd555468
**Summary**: Adds `resources/templates/list` handler to the MCP stdio + HTTP server, returning `{ "resourceTemplates": [] }`. Fixes a spec violation where compliant MCP clients received `-32601 Method not found` when probing the `resources` capability. Pure addition: one new dispatch arm in `protocol.rs`, one new helper in `resources.rs`, three tests, docs updated in English + zh-CN.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (mcp_server/protocol.rs, mcp_server/resources.rs), Docs (gitbooks, TEST-COVERAGE-MATRIX.md)
**Red flags**: None
**Linked issues**: Closes #2826
**PR-Issue alignment**: All 5 acceptance criteria from #2826 met — handler wired end-to-end, cursor params tolerated, 3 tests (helper + 2 wire-through), docs updated, coverage gate passes.
**CodeRabbit dedup**: CodeRabbit approved with no inline comments — nothing to dedup.
**Resolution actions**: None
**Surrounding code patterns checked**: resources/list and resources/read dispatch arms in protocol.rs — new arm matches their shape exactly.
**Dependency audit**: No new dependencies.
**Test coverage**: 3 new tests: list_resource_templates_returns_empty_array (unit helper), resources_templates_list_returns_empty_array (wire-through), resources_templates_list_ignores_unknown_params (cursor tolerance). All CI coverage gates green.
**Impact scan**: Pure addition — no existing callers affected, unimplemented-method fallback arm unchanged.
**Findings**:
- [minor] src/openhuman/mcp_server/resources.rs:183 — `list_resource_templates_result()` emits its own `log::debug!` trace while `protocol.rs` already logs the request entry, producing two debug lines per call. The count will always be 0 (static catalog), so the helper-level trace adds no runtime information. Worth removing in a follow-up to avoid log noise.
**Action taken**: APPROVED
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2828#pullrequestreview-4380607343

## AI Summary
- **What it does**: Adds the `resources/templates/list` MCP method handler, returning an empty `resourceTemplates` array. Fixes a spec violation where MCP clients (Claude Desktop, Cursor, Zed) received `-32601 Method not found` when probing the advertised `resources` capability, polluting their debug surfaces.
- **Breaking risk**: Zero. Pure addition — only the new named arm is added; the unimplemented-method default arm still handles all other unknown methods.
- **Security risk**: Zero. Returns a static empty array. No new data exposure, no new input surfaces.
- **Bottom line**: Safe to merge.
